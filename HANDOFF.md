# HANDOFF — Laser System Phase 31 FINAL

## 29. Change Log — تحديث الجلسة الحالية (Auto-Update للإحصائيات عبر Socket.io — القسم 13)

### ملاحظة هامة قبل البدء
طلب المستخدم في هذه الجلسة "صور تقريبية للحالة النهائية" كمرجع، لكن **لم يتم استلام أي صورة فعليًا** مع الرسالة — فقط ملف `7.zip` بالكود. تم المتابعة بالترتيب المحدد في القسم 18 والخطوة التالية الموثّقة في آخر Change Log (§28) بدون مرجع بصري جديد. إذا كانت هناك صور فعلية لم تُرفَع بنجاح، يُرجى إعادة إرسالها في رسالة لاحقة ليتم مطابقة أي تفاصيل شكلية (القسم 12) عليها.

### ما تم تنفيذه (القسم 13 من الـSpec — البند المتبقي الوحيد غير المنفَّذ سابقًا حسب §28)
1. **لم يُعَد بناء Socket.io** — تم استخدام نفس اتصال `socketRef` الموجود أصلاً في `App` (نفس الـSocket المستخدم لتحديث الماكينات/اللوجات/الإشعارات)، وتمريره كـ prop جديد `socket` إلى `StatisticsPage` فقط عند فتحها (`page==="statistics"`).
2. **`StatisticsPage`**: أُضيف `useEffect` جديد يستمع لحدث `machine:update` (المبثوث أصلاً من الباك إند عند أي تغيير في ملفات الماكينات — بما فيها إدخال/تعديل File Time عبر `PATCH /files/:fileId/time`، والحذف الفردي/الجماعي، وCleanup، ونقل WAITING) ويعيد استدعاء `load()` الحالية تلقائيًا، مع **Debounce بسيط (800ms)** لتفادي تكرار الطلب عند وصول عدة أحداث متتالية سريعة (مثال: Cleanup يحذف عدة ملفات دفعة واحدة فيبث عدة `machine:update`).
3. **لم يتغيّر الـBackend إطلاقًا** — الحدث `machine:update` كان مبثوثًا مسبقًا لكل نقاط تعديل الملفات (`broadcastMachineUpdate`)، فلم تكن هناك حاجة لأي حدث جديد أو تعديل في `statistics.js`/`machines.js`.
4. **لم يتغيّر منطق التحميل اليدوي** — تغيير الفلاتر (الفترة/الأسبوع/الشهر/الماكينة/المشغّل) ما زال يعمل بنفس الطريقة تمامًا (`useEffect(()=>{load();},[load])`)، والتحديث اللحظي الجديد إضافة فوقه فقط.

### الاختبارات المنفَّذة
- مراجعة يدوية دقيقة لتوازن الأقواس/الصياغة في منطقة التعديل (لا توجد بيئة `@babel/standalone` متاحة محليًا في هذه الجلسة بسبب عدم توفر اتصال شبكة لتثبيت الحزمة — لم يتم تنفيذ Babel transform فعلي كما في الجلسات السابقة؛ **هذا قيد جديد يجب توثيقه** ولا يُعتبر بديلاً كافيًا عنه).
- `node --check` على كل ملفات الباك إند (بدون أخطاء) — لم تتغيّر أي منها فعليًا في هذه الجلسة على أي حال.
- **لم يتم** Live Testing فعلي (لا وصول لسيرفر/DB حي في هذه الجلسة) — يبقى ضروريًا: التأكد عمليًا أن صفحة الإحصائيات تتحدّث تلقائيًا خلال ثوانٍ من تغيير ملف في يوم/أسبوع آخر (مثلاً إدخال File Time أو Cleanup من متصفح آخر)، بدون الحاجة لتغيير الفلاتر يدويًا أو تحديث الصفحة.

### الملفات المعدَّلة في هذه الجلسة
- `frontend/index.html` فقط: `StatisticsPage({addToast,socket})` + `useEffect` جديد للاستماع لـ`machine:update`، وتمرير `socket={socketRef.current}` عند استدعاء `<StatisticsPage/>` من `App`. **لا تعديل على الـBackend ولا على قاعدة البيانات.**

### Database Changes
لا يوجد.

### Safe to Deploy
NO — نفس القيد العام من كل الجلسات السابقة (لا Live Testing بعد). هذا التعديل بالذات منخفض الخطورة (استماع لحدث موجود مسبقًا + Debounce، لا منطق حذف/كتابة جديد)، لكنه **لم يُختبَر عبر Babel transform فعلي** في هذه الجلسة تحديدًا بسبب قيد الشبكة، فيُنصح بمراجعة بصرية إضافية للكود قبل النشر رغم انخفاض الخطورة العام.

### الخطوة التالية المقترحة (بالترتيب — كل بنود القسم 18 الأساسية منفَّذة الآن)
تبقّى فعليًا:
1. **Live Testing شامل** لكل شيء المذكور في كل الجلسات (الأولوية القصوى قبل أي استخدام حقيقي) — لم يحدث بعد في أي جلسة.
2. تشغيل كل ملفات الـMigration فعليًا على نسخة تجريبية من قاعدة البيانات (`migration_day_week_independence.sql`، `migration_file_time_statistics.sql`، وغيرها) قبل أي Live Testing.
3. توزيع الإحصائيات بالساعة + جدول جلسات تفصيلي (تفصيل اختياري لمطابقة الصورة المرجعية بدقة أكبر، القسم 12) — لم يُنفَّذ بعد.
4. تأكيد المستخدم الصريح بخصوص قرار القسم 5 (تسمية زر التسجيل) الموثّق في §28، إن كان يريد شيئًا مختلفًا.
5. إعادة إرسال الصور المرجعية إن كانت لم تُستلَم فعليًا (انظر الملاحظة أعلى هذا القسم).

## 1. الهدف
هذا الملف هو المرجع الرسمي لاستكمال مشروع Web App لإدارة ملفات ماكينات CNC/Laser.
الموقع يعمل فعليًا على السيرفر. لا إعادة بناء، لا Redesign، ولا Features جديدة غير مطلوبة.

## 2. Architecture
- Frontend: React 18، CDN + Babel Standalone، `frontend/index.html`
- Backend: Node.js + Express
- Database: MySQL / MariaDB
- Real-time: Socket.io
- Authentication: JWT + bcryptjs
- Upload: Multer
- Storage: Cloudflare R2 عند توفر Environment Variables، مع Local fallback
- Roles: Root Admin / Admin / Operator
- Machines: Big / Small / Queue (Waiting)

## 3. الميزات الموجودة التي يجب الحفاظ عليها
Login/Logout، Users، Machines، Files، Upload، Add by name، Rename، File time، File status، Delete، Drag & Drop، Move، Thumbnails، Text comments، Voice comments، Notifications، Logs، Downloads، Socket.io، R2، Permissions، Bulk Actions، Ctrl+V Thumbnail.

## 4. جزء الملفات — LOCKED
**ممنوع تعديل هذا الجزء.** الجزء الظاهر في صفوف الملفات سيظل كما هو:
- اسم الملف
- Thumbnail
- التعليق
- الصوت
- حالة الملف
- وقت الملف
- الإجراءات
- Checkbox
- Drag & Drop
- ترتيب الملفات
- منطق حالات الملفات

خصوصًا: `selectedFileIds` للـBulk فقط، و`pasteTargetId` لملف واحد فقط.
حالات الملفات مثل WAITING / WORKING / CUTTING وغيرها لا يتم تغييرها.
وقت الملف يدخله المشغل يدويًا بعد الانتهاء.

## 5. التحكم في الماكينات
الموقع **لا يتصل فعليًا بالماكينات ولا يتحكم بها**. هو نظام إدارة ملفات وتوزيعها وتسجيل أوقات العمل والإحصائيات.
لذلك المطلوب إزالة/تعطيل **أزرار تشغيل وإيقاف الماكينة نفسها** وأي Machine ON/OFF control غير ضروري.
**لا تحذف أو تعدل File Status.**
إذا كان زر في الكود خاصًا بحالة الملف وليس Machine Control، يبقى.

## 6. الصفحة الرئيسية — 7 أيام
الأيام بالترتيب:
**الجمعة → السبت → الأحد → الإثنين → الثلاثاء → الأربعاء → الخميس**

عند اختيار يوم تظهر بيانات هذا اليوم فقط.

كل يوم مستقل، وليس Filter لنفس الملفات.
كل `Week + Day` مستقل عن غيره.

لكل يوم 3 أماكن:
1. الماكينة الكبيرة
2. الماكينة الصغيرة
3. قائمة الانتظار

## 7. الأسابيع
اختيار:
- الأسبوع الأول
- الأسبوع الثاني
- الأسبوع الثالث
- الأسبوع الرابع

`Week 1 Friday` مختلف عن `Week 2 Friday`.
لا يتم نسخ ملفات أسبوع إلى آخر تلقائيًا.

## 8. نقل WAITING
إضافة زر **نقل الانتظار لليوم التالي**.
ينقل ملفات WAITING نفسها بدون Duplicate مع الحفاظ على بيانات الملف، Thumbnail، Comments، Voice، File Time، وFile Status حسب النظام الحالي.
الخميس ينتقل إلى **الجمعة من الأسبوع التالي**.

## 9. Cleanup
إضافة خيار **مسح ملفات أيام الأسبوع** مع Confirmation.
الأيام السبعة تكون محددة افتراضيًا، والمستخدم يلغي فقط الأيام التي يريد الاحتفاظ بها.

القرار النهائي:
**يتم حذف كل الملفات في الأيام المحددة بدون أي استثناء، بما فيها WAITING وWORKING وCUTTING.**

لكن:
**Cleanup لملفات العمل لا يحذف التاريخ الإحصائي.**

## 10. الإحصائيات
الإحصائيات يجب أن تعمل تلقائيًا من بيانات Database، ولا تحتاج رفعًا يدويًا أو Excel.

المشغل ينتهي من الملف ثم يدخل File Time يدويًا، وهذا الوقت يدخل في حساب الإحصائيات.

يجب دعم:
- يومي عند الحاجة
- أسبوعي
- شهري

عند اختيار شهري يمكن اختيار شهر مثل سبتمبر 2026.

لا نضيف Monthly Archive معقدًا إلا إذا ثبتت الحاجة إليه. البيانات التاريخية تبقى في Database.

## 11. فلاتر الإحصائيات
- الفترة: أسبوعي / شهري
- الأسبوع أو الشهر حسب الاختيار
- الماكينات: من الماكينات المسجلة فعليًا
- المشغلون: من Users المسجلين فعليًا، وليس أسماء ثابتة في الكود

اختيار مشغل يجب أن يفلتر الإحصائيات الخاصة به.

## 12. شكل الإحصائيات المطلوب
قريب من التصميم المرجعي المرسل:
- أفضل ماكينة
- إجمالي تشغيل الماكينات
- متوسط التشغيل اليومي
- إجمالي جلسات المشغلين
- توزيع التشغيل اليومي لكل ماكينة
- متوسط التشغيل اليومي لكل ماكينة
- ساعات تشغيل الماكينات خلال الأسبوع
- جدول جلسات المشغلين حسب البيانات الموجودة

لا تستخدم أرقامًا ثابتة Hard-coded.

## 13. Auto Update
استخدم Socket.io الحالي عند الإمكان:
Database → Socket.io → تحديث الإحصائيات.
لا تعِد بناء Socket.io.

## 14. Bulk Actions
Admin / Root Admin فقط:
- selectedFileIds
- Checkbox
- Select All
- Indeterminate
- Bulk Action Bar
- Bulk Move
- Bulk Delete
- Clear Selection

Operator لا يرى Bulk UI ولا يستطيع Bulk API يدويًا.
يجب التحقق من IDs والتعامل مع Storage وLogs/Notifications/Socket حسب النظام الحالي.

## 15. Ctrl+V
الحفاظ على:
- `selectedFileIds` للـBulk فقط
- `pasteTargetId` لملف واحد
- منع duplicate uploads
- stable target
- lock مثل `isPastingRef`
- نجاح Paste يحدث الملف المستهدف فقط
- لا يغير Bulk Selection
- الفشل يحرر lock ولا يفسد selection/target

## 16. التصميم
لا تغيّر:
- الألوان
- Layout
- Login
- Sidebar
- Tables
- WorkBar
- Branding
- شكل صفوف الملفات

لا Redesign.

## 17. Database
لا تغيّر Schema/Migration إلا عند وجود مشكلة حقيقية أو نقص مثبت.
قبل أي تغيير:
1. اشرح المشكلة.
2. حدد السبب والجدول/الحقل.
3. اقترح أقل تعديل.
4. اشرح التأثير.
5. لا تنفذ تغييرًا جوهريًا قبل عرضه.

## 18. ترتيب التنفيذ
1. تحليل الكود الحالي بدون تعديل.
2. تحديد نموذج Week + Day الحالي.
3. تنفيذ/ضبط استقلال الأيام.
4. WAITING → Next Day.
5. Week 1–4.
6. Cleanup.
7. Weekly/Monthly Statistics.
8. Operator filter من Users.
9. Auto-update عبر Socket.io.
10. Regression testing.

## 19. الاختبارات
اختبر:
- الأيام السبعة وتغيير اليوم والعودة لليوم الحالي
- استقلال الأيام والأسابيع
- Week 1/2/3/4
- منع القفز غير المقصود
- WAITING → اليوم التالي
- Thursday → Next Week Friday
- عدم Duplicate
- الحفاظ على بيانات الملف
- Cleanup وConfirmation واختيار الأيام
- حذف كل حالات الملفات في الأيام المختارة
- عدم حذف التاريخ الإحصائي
- Weekly/Monthly Statistics
- Machine/Operator filters
- Operators الحقيقيون من Users
- Bulk Actions والصلاحيات
- Ctrl+V وعدم تكراره
- Login/Logout/Users/Upload/Add/Rename/File Time/File Status/Delete/Move/Drag&Drop/Thumbnail/Comments/Download/Notifications/Logs/Socket.io
- Operator/Admin/Root permissions

## 20. Bug Report
لكل Bug:
### Bug
ما الذي يحدث؟
### Expected
ما المفترض؟
### Cause
سبب المشكلة في الكود.
### Fix
أقل تعديل ممكن.
### Risk
تأثيره على Features أخرى.

## 21. FINAL STATUS
### Completed
- **استقلال الأيام السبعة + الأسابيع الأربعة (المرحلة 1 من 8)**:
  - Migration جديد `migration_day_week_independence.sql`: يحوّل `work_weeks`/`work_days` من نموذج "تاريخ حقيقي + يوم نشط واحد عالمي" إلى شبكة ثابتة 28 خانة (4 أسابيع × 7 أيام من الجمعة للخميس)، كل خانة لها id دائم ومستقل، مع Seed تلقائي.
  - Backend: `workdays.js` أُعيد بناؤه بالكامل — `GET /api/work/grid` (الشبكة كاملة) و`GET /api/work/weeks`. أُزيلت المسارات القديمة (`/days`, `/days/active`, `POST /weeks`) لأنها لم تعد ذات معنى في النموذج الجديد.
  - Backend: `machines.js` — كل نقطة تتعامل مع ملفات (عرض `GET /`, إضافة بالاسم، رفع، صورة معاينة، تعليق كتابي/صوتي، نقل فردي/جماعي، حذف فردي/جماعي، إعادة ترتيب) أصبحت تُفلتر/تُخزّن `work_day_id` بشكل صحيح — كل خانة (أسبوع×يوم) معزولة تمامًا عن غيرها كما هو مطلوب.
  - Frontend: أُضيف Header جديد (تابات الأيام السبعة + Dropdown الأسبوع) أسفل الـTop Bar مباشرة، ظاهر في صفحتي المشرف والمشغّل، مطابق للصور المرجعية المرسلة. `AdminTable` و`Api` كل دوالهما المتعلقة بالملفات بقت بتاخد/بتبعت `workDayId` الحالي.
  - أُزيلت صفحة "أيام العمل" (WorkDaysPage) القديمة بالكامل من الـSidebar والراوتنج، لأن نموذجها (يوم نشط واحد بتاريخ حقيقي) لم يعد موجودًا في الداتابيز أصلاً.
  - تم التحقق: JS/JSX بالكامل يمرّ Babel transform بدون أخطاء Syntax، وكل ملفات الباك إند تمرّ `node --check` بدون أخطاء. لم يتم تشغيل اختبار حي فعلي على سيرفر (لا يوجد وصول لبيئة Live في هذه الجلسة).

### Needs Testing (Live، لم يُنفَّذ بعد في هذه الجلسة)
- تسجيل الدخول، تحميل الشبكة، التنقل بين الأيام السبعة والأسابيع الأربعة، والتأكد أن كل خانة تعرض ملفاتها فقط.
- إضافة/رفع ملف، نقل فردي/جماعي، حذف فردي/جماعي، إعادة ترتيب Drag&Drop — كل هذا داخل خانة يوم واحدة والتأكد من عدم تسرّب بيانات بين الأيام.
- تشغيل/إيقاف الماكينة (Toggle) مع التأكد أن قائمة الملفات المعروضة لا تُفرَّغ لحظيًا في المتصفحات الأخرى المفتوحة على يوم مختلف.
- Regression على باقي الميزات المحمية (Thumbnails، الصوت، Ctrl+V، الصلاحيات، Socket.io، R2).

### Bugs / قيود معروفة (لم تُصلَح بعد — مقصودة ومؤجَّلة لمراحل لاحقة)
- **صفحة الإحصائيات (Weekly) معطّلة مؤقتًا**: `statistics.js` (weekly scope) يعتمد على `start_date`/`end_date` حقيقيين للأسبوع لفلترة `machine_runtime_logs`/`operator_sessions`، وهذان الحقلان أصبحا NULL افتراضيًا في الأسابيع الأربعة الثابتة الجديدة. الإحصائيات الشهرية (Monthly) غير متأثرة لأنها تعتمد على شهر/سنة حقيقيين فقط. **السبب**: تعمّدت عدم لمس منطق الإحصائيات في هذه الجلسة لأن المستخدم اختار "استقلال الأيام" كأول مرحلة تُنفَّذ (وليس الإحصائيات — مرحلة 7 في الترتيب). **الإصلاح المخطط**: في مرحلة الإحصائيات، إعادة تصميم الفلترة الأسبوعية لتعتمد على week_number المُختار بدل مدى تاريخ حقيقي (كما هو موضح في القسم 10-13 أعلاه).
### Database Changes
`migration_day_week_independence.sql` — انظر تفاصيله في "Completed" أعلاه. لم تُطبَّق على أي سيرفر فعلي؛ الملف جاهز للتشغيل يدويًا على قاعدة البيانات الحقيقية.
### Files Changed
- `migration_day_week_independence.sql` (جديد)
- `backend/src/routes/workdays.js` (إعادة بناء كامل)
- `backend/src/routes/machines.js` (تعديل جميع نقاط الملفات)
- `frontend/index.html` (Api object، AdminTable، AdminDash، App — Header جديد، إزالة WorkDaysPage)
### Safe to Deploy
NO — لم يُختبَر حيًا على سيرفر/قاعدة بيانات فعلية بعد، ولم تُطبَّق الـMigration على أي بيئة. لازم Live Testing أولاً + تشغيل الـMigration يدويًا + مراجعة قيد الإحصائيات أعلاه.
### Recommended Next Step
1. تشغيل `migration_day_week_independence.sql` على نسخة تجريبية من قاعدة البيانات أولاً.
2. Live Testing للتنقل بين الأيام/الأسابيع وعمليات الملفات.
3. الانتقال للمرحلة التالية في الترتيب (القسم 18): "نقل WAITING لليوم التالي" ثم "Week 1-4" ثم Cleanup... إلخ، أو معالجة قيد الإحصائيات المذكور أعلاه إن رغب المستخدم بتقديمها.

## 30. Change Log — تحديث الجلسة الحالية (القسم 5 — تنفيذ فعلي: إزالة/تعطيل أزرار تشغيل/إيقاف الماكينة)

### القرار (بناءً على طلب صريح من المستخدم بالانتقال للخطوة التالية بعد §29)
بعد فصل Statistics عن `machine_runtime_logs` لتعتمد على File Time (Change Log §29)، زال التعارض الوحيد الذي كان يمنع تنفيذ §5 حرفيًا (كان مسجَّلًا في §28: حذف الزر كان سيُفرغ Statistics من مصدرها الوحيد وقتها). بما أن هذا لم يعد صحيحًا الآن، نُفِّذ §5 فعليًا هذه الجلسة: **إزالة/تعطيل** زر "تسجيل بدء/توقف التشغيل" (Machine ON/OFF control)، بدل مجرد إعادة تسميته كما كان في §28.

### ما تم تنفيذه
1. **`frontend/index.html` (`AdminTable`)**: زر "تسجيل بدء التشغيل"/"تسجيل توقف التشغيل" **أُزيل بالكامل** من الواجهة (لم يعد يظهر إطلاقًا لأي دور). الدالة `toggle()` المرتبطة به حُذفت أيضًا (كانت تستدعي `Api.toggleMachine` فقط، ولا مستدعٍ آخر لها). `Api.toggleMachine` نفسها **بقيت معرَّفة** في كائن `Api` (بلا استدعاء من أي مكان — كود غير مستخدَم لكن غير مؤذٍ، أقل تدخل ممكن). سطر عرض الحالة "الحالة: قيد التشغيل/متوقفة" **بقي كما هو دون تغيير** — هو عرض معلوماتي فقط (ليس تحكمًا)، سيبقى مجمَّدًا على آخر قيمة له من الآن فصاعدًا لعدم وجود أي طريقة لتغييره بعد إزالة الزر — هذا متوقَّع وليس Bug.
2. **`backend/src/routes/machines.js` (`PATCH /:code/toggle`)**: المسار **لم يُحذف**، لكن **عُطِّل بالكامل** — أول سطر فيه يرجّع مباشرة `410 Gone` مع رسالة توضيحية، بدون تنفيذ أي من المنطق القديم (لا تحديث `machines.status`/`running_started_at`، ولا إدراج في `machine_runtime_logs`، ولا Log/Notification/Socket). كل المنطق القديم **حُذف من الملف فعليًا** (وليس معلَّقًا فقط) لتفادي كود ميت مربك لجلسات لاحقة — تفاصيله محفوظة في نسخة الملف السابقة (ملف zip الجلسة الماضية) لو احتاج المستخدم الرجوع إليه.
3. **`machine_runtime_logs` (الجدول نفسه في قاعدة البيانات) لم يُحذف ولم يُعدَّل** — أي بيانات تاريخية قديمة فيه (لو وُجدت من قبل هذه الترقية) تبقى موجودة فيه فقط كسجل تاريخي ميت، غير مقروء من أي مسار API بعد الآن (لا `/toggle` يكتب إليه، ولا `/statistics/machines` يقرأ منه — كلاهما مؤكَّد من جلستين منفصلتين).
4. **لم يتأثر إطلاقًا**: File Status (WAITING/WORKING/CUTTING/DELIVERED) وأزراره، File Time وطريقة إدخاله، `machine_time` (مجموع أوقات الملفات المحذوفة/المكتملة)، `file_time_archive`/`time_recorded_at` من §29، Bulk Actions، Ctrl+V، Drag&Drop، Cleanup، نقل WAITING، Login/Sidebar/Tables/Branding.

### الاختبارات المنفَّذة
- `node --check` على `machines.js` (بدون أخطاء) بعد كل تعديل.
- فحص توازن الأقواس `{}`/`()` يدويًا على كامل `frontend/index.html` بعد التعديل (متوازنة تمامًا: 0/0) — تعذّر تشغيل Babel transform فعليًا في هذه الجلسة تحديدًا (لا يوجد `@babel/core`/`node_modules` مثبَّتة محليًا ولا اتصال شبكة متاح لتثبيتها) — **هذا قيد بيئة هذه الجلسة فقط**، وليس تخطيًا متعمَّدًا لخطوة اختبار كانت متاحة سابقًا؛ يُنصَح بجلسة لاحقة تملك بيئة Node كاملة بتشغيل Babel transform الفعلي على `frontend/index.html` كخطوة تحقق إضافية قبل أي نشر حقيقي.
- **لم يتم** Live Testing فعلي (لا وصول لسيرفر/DB حي) — يبقى ضروريًا: التأكد أن الزر اختفى فعليًا من واجهة كل من الماكينة الكبيرة والصغيرة لكل من المشرف والمشغّل، وأن استدعاء `PATCH /api/machines/:code/toggle` مباشرة (لو حاول أحد يدويًا) يرجّع 410 فعليًا بدون أي أثر جانبي على `machines`/`machine_runtime_logs`، وأن باقي الميزات (خصوصًا Statistics من §29) ما زالت تعمل بلا أي تغيير.

### الملفات المعدَّلة في هذه الجلسة
- `frontend/index.html` (`AdminTable`: حذف زر التشغيل/الإيقاف ودالة `toggle`)
- `backend/src/routes/machines.js` (`PATCH /:code/toggle`: تعطيل كامل بردّ 410، حذف المنطق القديم، تعليقات توثيقية محدَّثة)

### Database Changes
لا يوجد. لا Schema جديد ولا Migration جديد في هذه الجلسة — `machine_runtime_logs` لم يُمَسّ إطلاقًا (لا حذف جدول، لا حذف بيانات).

### Safe to Deploy
NO — نفس القيد العام من كل الجلسات السابقة (لا Live Testing على سيرفر/DB حقيقية بعد). هذا التعديل تحديدًا منخفض المخاطر (إزالة/تعطيل تحكم لم يكن حقيقيًا أصلاً)، لكن يبقى يتطلب تحقق أن لا Regression على أي شيء آخر.

### الخطوة التالية المقترحة
كل بنود §18 الأساسية نُفِّذت الآن فعليًا (استقلال الأيام، WAITING→Next Day، Week1-4، Cleanup، إصلاح الإحصائيات ليعتمد على File Time، وإزالة/تعطيل تحكم الماكينة). المتبقي فعليًا:
1. **Live Testing شامل** لكل شيء نُفِّذ عبر كل الجلسات — الأولوية القصوى الآن قبل أي استخدام حقيقي، وتشغيل `migration_file_time_statistics.sql` على قاعدة بيانات فعلية أولًا.
2. Auto-update الإحصائيات عبر Socket.io (تفصيل اختياري، §13).
3. توزيع الإحصائيات بالساعة + جدول جلسات تفصيلي (تفصيل اختياري لمطابقة الصورة المرجعية بدقة أكبر، §12).

## 29. Change Log — تحديث الجلسة الحالية (تصحيح نهائي لمصدر الإحصائيات — File Time بدل Runtime Logs)

### قرار المستخدم (حرفيًا، مُلزِم لأي جلسة لاحقة)
"وقت تشغيل الماكينات" في Statistics يجب أن يساوي مجموع "وقت الملف" (time_seconds) الفعلي
الموجود في ملفات الماكينة — أي نفس الرقم الظاهر في بطاقة الماكينة تحت "مجموع وقت الملفات".
مثال مُلزِم: 01:20:00 + 00:50:00 = 02:10:00.
- **ممنوع** استخدام `machine_runtime_logs` أو زر تسجيل بدء/توقف التشغيل كمصدر لأي رقم في Statistics بعد الآن.
- **ممنوع** تعديل File Time أو File Status أو طريقة إدخال الوقت (نفس الحقل، نفس الـAPI الظاهري، نفس الواجهة تمامًا كما كانت).
- Statistics يجب أن تجمع File Time فعليًا حسب اليوم/الأسبوع/الشهر والماكينة، وتُطبَّق فلاتر الماكينة والمشغل بشكل صحيح.
- **لا تحذف** `machine_runtime_logs` ولا زر التشغيل/الإيقاف الآن — يبقيان موجودَين كما هما، فقط فُصِلا عن حساب Statistics. القرار بشأن مصيرهما مؤجَّل لخطوة قادمة منفصلة (مراجعة القسم 5) لم تُنفَّذ بعد في هذه الجلسة.

### التعارض الذي تم تحليله قبل التنفيذ (كما يلزم القسم 17/22)
`machine_files.work_day_id` يشاور على شبكة أيام/أسابيع **وهمية بالكامل** (Week1-4 × fri-thu، بلا `work_date` حقيقي — انظر `migration_day_week_independence.sql`)، ولا يوجد أي حقل زمني حقيقي آخر على صف الملف يمكن التجميع عليه حسب يوم/أسبوع/شهر تقويمي فعلي. كذلك صف `machine_files` **يُحذَف فعليًا** من القاعدة عند: حذف فردي، حذف جماعي، وCleanup (القسم 9) — فلو اعتمدت Statistics على `machine_files` الحيّة فقط، فسيختفي أي وقت ملف من الإحصائيات التاريخية بمجرد حذف الملف، وهذا يتعارض مع القرار الثابت مسبقًا "Cleanup لا يحذف التاريخ الإحصائي" (القسم 9).
**الحل المطبَّق (أقل تعديل ممكن لحل التعارض، وليس تجاهله):**
1. عمود جديد `machine_files.time_recorded_at` (DATETIME، NULL) — يُحدَّث تلقائيًا بـ`NOW()` **فقط** داخل `PATCH /files/:fileId/time` الموجود أصلاً، بدون أي تغيير على Body الطلب أو الاستجابة أو شكل الحقل في الواجهة. هذا هو التاريخ الحقيقي الوحيد المتاح لحظة إدخال/تعديل وقت الملف، ويُستخدَم فقط للتجميع الزمني في Statistics.
2. جدول أرشيف جديد `file_time_archive` (id, machine_id, seconds, recorded_at, archived_at) — سطر Append-only يُكتَب **قبل** حذف أي صف `machine_files` له `time_seconds > 0`، في **الحذف الفردي، الحذف الجماعي، وCleanup** الثلاثة (نفس القيمة `time_recorded_at` أو `created_at` كبديل لو لم يُدخَل وقت صراحةً). هذا يحفظ مساهمة الملف في الإحصائيات حتى بعد حذف صفه الأصلي — تمامًا كما كان `machine_runtime_logs` يحفظ التاريخ سابقًا، لكن بمصدر File Time بدل Runtime.
3. `GET /api/statistics/machines` أعاد بناءه بالكامل ليجمع `SUM(seconds)` من **مصدرين معًا**: `machine_files` الحيّة (`time_seconds > 0`, بحسب `COALESCE(time_recorded_at, created_at)`) + `file_time_archive` (بحسب `recorded_at`) — كلاهما مقيَّد بنفس مدى `resolveRange` (أسبوع تقويمي حقيقي جمعة→خميس أو شهر حقيقي، **لم يتغيّر منطقه إطلاقًا**) وفلتر `machineCode` الاختياري كما كان.
4. لم يتغيّر أي شيء آخر: شكل استجابة `/machines` نفسه تمامًا (`{label, weekStart, machines:[{code,label,totalSeconds,total,runs}], daily:[{date,big,small}]}`) — فالواجهة (`StatisticsPage` في `frontend/index.html`) **لم تُلمَس إطلاقًا** ولا تحتاج أي تعديل. `GET /api/statistics/operators` (إحصائيات جلسات المشغّلين من `operator_sessions`) لم يُلمَس أيضًا — القرار يخص فقط "وقت تشغيل الماكينات".

### ما تم تنفيذه
1. **Migration جديد**: `migration_file_time_statistics.sql` — عمود `machine_files.time_recorded_at` + جدول `file_time_archive` + Backfill لصفوف قديمة موجودة (`time_seconds > 0` بلا `time_recorded_at`) من `created_at`. لم يُطبَّق على أي سيرفر فعلي بعد؛ جاهز للتشغيل اليدوي. `database.sql` (للتنصيب الجديد من الصفر) عُدِّل بنفس الإضافتين مباشرة.
2. **`backend/src/routes/machines.js`**:
   - `PATCH /files/:fileId/time`: يضبط `time_recorded_at = NOW()` بجانب `time_seconds` (سطر SQL واحد إضافي فقط، لا تغيير آخر).
   - حذف فردي (`DELETE /files/:fileId`)، حذف جماعي (`DELETE /files/bulk-delete`)، Cleanup (`DELETE /files/cleanup`): كل واحد منها الآن يُدرِج سطرًا في `file_time_archive` قبل حذف صف الملف مباشرة (فقط لو `time_seconds > 0`) — نفس المنطق مكرر 3 مرات بأقل تغيير ممكن، بدون أي دالة مشتركة جديدة تمس أي مسار آخر.
   - تعليقات توثيقية محدَّثة فوق `/:code/toggle` وCleanup لتوضيح أن `machine_runtime_logs` لم يعد مصدر الإحصائيات (بدون أي تغيير في السلوك البرمجي لهما).
3. **`backend/src/routes/statistics.js`**: إعادة بناء `GET /machines` بالكامل (تفاصيل في "التعارض" أعلاه). `resolveRange`/`resolveWeekStart`/`GET /operators` — **بلا أي تغيير**.
4. **لم يُمَس**: `frontend/index.html` (صفر تغييرات)، File Status، طريقة إدخال الوقت من الواجهة، `machine_runtime_logs`، زر تسجيل بدء/توقف التشغيل، `operator_sessions`، Cleanup UI/Modal، أي Bulk/Ctrl+V/Drag&Drop منطق.

### الاختبارات المنفَّذة
- `node --check` على `machines.js` و`statistics.js` (بدون أخطاء) بعد كل تعديل.
- محاكاة يدوية (Node script منفصل، مطابقة لنفس منطق التجميع داخل `/machines`) بمدخلات `01:20:00` (صف حيّ) + `00:50:00` (صف مؤرشَف) على نفس اليوم → النتيجة: **`02:10:00`** بالضبط (مطابق تمامًا للمثال الذي حدده المستخدم)، و`runs: 2`.
- **لم يتم** Live Testing فعلي (لا وصول لسيرفر/DB حي في هذه الجلسة) — يبقى ضروريًا: تشغيل `migration_file_time_statistics.sql` فعليًا، ثم التأكد عمليًا أن رقم Statistics لأي ماكينة/يوم/أسبوع/شهر يطابق تمامًا مجموع "وقت الملف" الظاهر في بطاقة الماكينة لنفس الفترة، وأن حذف ملف (فردي/جماعي/Cleanup) لا يُنقِص هذا الرقم التاريخي، وأن فلتر `machineCode` ما زال يعمل، وأن صفحة الإحصائيات لا تُظهر أي خطأ بعد التغيير (رغم عدم تغيير شكل الاستجابة).

### الملفات المعدَّلة في هذه الجلسة
- `migration_file_time_statistics.sql` (جديد)
- `database.sql` (عمود `time_recorded_at` + جدول `file_time_archive` للتنصيب الجديد)
- `backend/src/routes/machines.js` (PATCH time، 3 نقاط حذف، تعليقات توثيقية فقط بلا تغيير سلوكي في toggle/Cleanup)
- `backend/src/routes/statistics.js` (إعادة بناء `GET /machines` فقط)

### Database Changes
`migration_file_time_statistics.sql` — عمود جديد + جدول جديد (تفاصيل أعلاه). لم يُطبَّق على أي سيرفر فعلي بعد.

### Safe to Deploy
NO — يحتاج تشغيل الـMigration يدويًا أولًا، ثم Live Testing لمطابقة الأرقام كما هو موضح أعلاه، قبل الاعتماد الفعلي على صفحة الإحصائيات.

### الخطوة التالية المقترحة (بالترتيب — كما طلب المستخدم صراحةً التوقف هنا الآن)
تم التوقف هنا كما طلب المستخدم صراحةً، بدون أي تعديل إضافي بعد هذا القرار. الخطوة التالية المتفَق عليها صراحةً مع المستخدم هي العودة لموضوع "أزرار تشغيل/إيقاف الماكينة" (مراجعة قرار القسم 5 المطبَّق سابقًا في Change Log §28) — **لم تُنفَّذ في هذه الجلسة**، بانتظار طلب المستخدم صراحةً في جلسة لاحقة.

## 28. Change Log — تحديث الجلسة الحالية (القسم 5 — أزرار تشغيل/إيقاف الماكينة)

### تحليل التعارض (قبل أي تنفيذ، كما يلزم القسم 17/22)
القسم 5 يطلب: "إزالة/تعطيل أزرار تشغيل وإيقاف الماكينة نفسها وأي Machine ON/OFF control غير ضروري."
لكن بالفحص الفعلي لكود `PATCH /:code/toggle`، هذا الزر **لا يتحكم بجهاز فعلي إطلاقًا** (الموقع أصلاً لا يتصل بأي ماكينة حقيقية — لم يكن هناك تحكم حقيقي ليُزال). وظيفته الفعلية الوحيدة: تسجيل لحظة بدء/انتهاء دورة تشغيل في جدول `machine_runtime_logs`، وهو **المصدر الوحيد** لبيانات "وقت تشغيل الماكينات الفعلي" في صفحة الإحصائيات (القسم 10-13: أفضل ماكينة، إجمالي التشغيل، متوسط التشغيل اليومي، توزيع الأيام) — وهي نفس الميزة التي طلب المستخدم إصلاحها والحفاظ على عملها التلقائي بدون بيانات يدوية/Excel.
**حذف هذا الزر كليًا كان سيُفرغ صفحة الإحصائيات بالكامل من مصدر بياناتها**، فيتعارض تنفيذ حرفي للقسم 5 مع القسم 10-13 (وكلاهما جزء من نفس الـSpec).

### القرار المطبَّق في هذه الجلسة (حل وسط أدنى تعديل، وليس حذفًا)
بما أن الزر أصلاً **ليس تحكمًا فعليًا بجهاز** (لا يوجد جهاز متصل من الأساس)، فالمشكلة الحقيقية في القسم 5 هي التسمية المُضلِّلة ("تشغيل/إيقاف الماكينة" توحي بتحكم فعلي)، وليست الوظيفة نفسها (وهي "تسجيل" ضروري ومطلوب صراحة في القسم 10). لذلك:
- **لم يُحذف** الزر ولا الـEndpoint (لتفادي كسر الإحصائيات التي تم إصلاحها في جلسة سابقة).
- **أُعيدت تسميته فقط**: من "تشغيل" / "إيقاف" إلى **"تسجيل بدء التشغيل"** / **"تسجيل توقف التشغيل"**، مع إضافة `title` (Tooltip) صريح: "تسجيل بدء/انتهاء فترة تشغيل فعلية على هذه الماكينة (للإحصائيات فقط — الموقع لا يتحكم في الماكينة الفعلية)".
- أُضيف تعليق توثيقي في الباك إند (`machines.js`) يشرح هذا القرار عند مسار `/:code/toggle` مباشرة، حتى لا تُخطئ أي جلسة AI لاحقة وتحذفه ظنًا منها أنه Machine Control فعلي.
- لم يتغيّر أي سلوك برمجي في الـBackend (نفس الـEndpoint، نفس الجدول، نفس منطق الإحصائيات) — تعديل الجلسة كله Labels/Comments فقط، صفر مخاطرة على أي ميزة أخرى.

### ⚠️ يحتاج تأكيد المستخدم صراحة
هذا قرار اجتهادي لحل تعارض حقيقي في الـSpec نفسه، وليس تنفيذًا حرفيًا للقسم 5. **إذا كانت نيّة المستخدم الفعلية هي حذف الزر نهائيًا حتى لو أوقف تسجيل الإحصائيات** (مثلاً لأن التسجيل سيتم بطريقة أخرى لاحقًا لم تُحدَّد بعد)، فهذا يحتاج قرارًا صريحًا جديدًا، لأنه سيتطلب أيضًا حذف/تعطيل جزء من صفحة الإحصائيات نفسها بالتبعية (القسم 10-13) ولا يمكن تنفيذه بأمان دون توضيح المصدر البديل للبيانات.

### الاختبارات المنفَّذة
- `node --check` على `machines.js` (بدون أخطاء).
- Babel transform كامل على `frontend/index.html` (نجح بدون أخطاء).
- لا حاجة لـLive Testing إضافي هنا (تغيير Labels/Comments فقط، لا منطق جديد).

### الملفات المعدَّلة في هذه الجلسة
- `backend/src/routes/machines.js` (تعليق توثيقي فقط فوق `/:code/toggle`، لا تغيير منطقي)
- `frontend/index.html` (نص زر التشغيل/الإيقاف + `title` توضيحي في `AdminTable`)

### Database Changes
لا يوجد.

### Safe to Deploy
NO — نفس القيد العام من كل الجلسات السابقة (لا Live Testing بعد). هذا التعديل تحديدًا منخفض الخطورة جدًا (نص فقط) مقارنة بباقي الميزات (Cleanup تحديدًا الأخطر لأنها Destructive).

### الخطوة التالية المقترحة
تبقّى فعليًا فقط (كل بنود القسم 18 الأساسية نُفِّذت أو عولجت الآن):
1. **Live Testing شامل** لكل شيء (استقلال الأيام، الإحصائيات، نقل الانتظار، Cleanup، تسمية زر التسجيل) — الأولوية القصوى قبل أي استخدام حقيقي.
2. Auto-update الإحصائيات عبر Socket.io (تفصيل اختياري، القسم 13).
3. توزيع الإحصائيات بالساعة + جدول جلسات تفصيلي (تفصيل اختياري لمطابقة الصورة المرجعية بدقة أكبر، القسم 12).
4. مراجعة قرار القسم 5 أعلاه مع المستخدم إن كان يريد شيئًا مختلفًا عمّا نُفِّذ.

## 27. Change Log — تحديث الجلسة الحالية (Cleanup — مسح ملفات أيام الأسبوع)

### ما تم تنفيذه (القسم 9 من الـSpec)
1. **Backend**: مسار جديد `DELETE /api/machines/files/cleanup` (مشرف فقط)، Body: `{ workDayIds: [...], currentWorkDayId }`.
   - يجلب كل صفوف `machine_files` (بأي حالة — WAITING/WORKING/CUTTING/DELIVERED، بلا استثناء كما ينص القرار النهائي في §9) المرتبطة بأي من `workDayIds` المُرسَلة، عبر الماكينتين وقائمة الانتظار الثلاثة معًا.
   - لكل ملف: يحذف الملف الفعلي/الصورة المصغّرة/التسجيل الصوتي من R2 أو التخزين المحلي (نفس منطق الحذف الفردي/الجماعي الموجود مسبقًا بالضبط — لم يُنشأ منطق حذف تخزين جديد)، ثم يضيف وقته إلى `machines.machine_time` (نفس سلوك الحذف الفردي/الجماعي الحالي)، ثم يحذف صف `machine_files` نفسه.
   - **لا يمسّ إطلاقًا** `machine_runtime_logs` ولا `operator_sessions` — وهما المصدر الوحيد للإحصائيات (القسم 10-12) — فالتاريخ الإحصائي يبقى سليمًا 100% حتى بعد Cleanup الكامل، تمامًا كما يلزم القرار النهائي في §9.
   - Log + Notification بعدد الملفات المحذوفة الفعلي. بث تحديث Socket.io فقط لو اليوم الحالي المعروض (`currentWorkDayId`) كان ضمن الأيام المحذوفة (لتفادي إرسال حالة يوم لا يشاهده أحد الآن لعملاء آخرين — نفس القيد المعروف والموثّق مسبقًا في بث الـSocket لهذا المشروع).
2. **Frontend**: زر **"Cleanup"** (أحمر) بجانب قائمة اختيار الأسبوع في الهيدر، يظهر فقط للمشرف وهو في صفحة "admin". يفتح `CleanupModal`:
   - اختيار الأسبوع (افتراضيًا: الأسبوع المعروض حاليًا).
   - 7 Checkboxes للأيام، **كلها محددة افتراضيًا** (زر واحد "إلغاء الكل" و"تحديد الكل" لتسهيل الاستثناء اليدوي) — مطابق حرفيًا لـ"الأيام السبعة تكون محددة افتراضيًا، والمستخدم يلغي فقط الأيام التي يريد الاحتفاظ بها".
   - تحذير واضح داخل الـModal + `window.confirm` نصّي قبل التنفيذ يوضّح أن كل الحالات ستُحذف بلا استثناء وأن التاريخ الإحصائي لن يتأثر ولا يمكن التراجع.
   - بعد النجاح: Toast بعدد الملفات المحذوفة + إغلاق الـModal + تحديث القائمة الحالية إن كانت متأثرة.
3. لم يُمَس أي شيء في: منطق حالات الملفات، `selectedFileIds`/Bulk، Drag&Drop، الشكل العام، Login/Sidebar/Tables — القسم 4 و16 (Locked) — الـModal الجديد بتصميم يطابق ألوان/Style الموقع الحالي دون أي مكتبة UI خارجية جديدة.

### الاختبارات المنفَّذة
- `node --check` على كل ملفات backend (بدون أخطاء) بعد الإضافة.
- Babel transform كامل على `<script type="text/babel">` في `frontend/index.html` (نجح بدون أخطاء).
- **لم يتم** Live Testing فعلي (لا وصول لسيرفر/DB حي) — يبقى ضروريًا قبل الاعتماد: التأكد عمليًا أن Cleanup فعلاً يحذف كل الحالات في الأيام المختارة فقط (وليس أي يوم آخر)، وأن صفحة الإحصائيات (بعد Cleanup) ما زالت تعرض نفس الأرقام التاريخية بلا أي تغيير (تأكيد عملي لعدم مسّ `machine_runtime_logs`/`operator_sessions`).

### الملفات المعدَّلة في هذه الجلسة
- `backend/src/routes/machines.js` (مسار جديد `DELETE /files/cleanup`)
- `frontend/index.html` (`Api.cleanupFiles`، مكوّن جديد `CleanupModal`، زر Cleanup في هيدر صفحة المشرف، حالة `showCleanup` في `App`)

### Database Changes
لا يوجد. لا Schema جديد ولا Migration جديد في هذه الجلسة.

### Safe to Deploy
NO — نفس القيد من كل الجلسات السابقة: لم يُختبَر حيًا بعد على سيرفر/DB فعلية. **الميزة الجديدة هنا حسّاسة بشكل خاص لأنها تحذف بيانات بشكل دائم** — يُنصَح بشدة باختبارها أولًا على نسخة تجريبية من قاعدة البيانات قبل أي استخدام فعلي.

### الخطوة التالية المقترحة (بالترتيب من القسم 18 — تبقّى فقط)
1. Live Testing شامل لكل ما نُفِّذ في الجلسات الأربع (استقلال الأيام، إصلاح الإحصائيات، نقل الانتظار، Cleanup) — **أولوية قصوى قبل أي استخدام حقيقي**، خصوصًا Cleanup لأنه Destructive.
2. Auto-update الإحصائيات عبر Socket.io (القسم 13) — حاليًا الإحصائيات تُحمَّل فقط عند تغيير الفلاتر يدويًا، وليس Live.
3. توزيع الإحصائيات بالساعة + جدول الجلسات التفصيلي (تفاصيل إضافية اختيارية لمطابقة الصورة المرجعية بدقة أكبر من القسم 12).
4. مراجعة قسم 5 (إزالة أزرار تشغيل/إيقاف الماكينة نفسها) — لم تُلمَس في أي من الجلسات الأربع بعد؛ `router.patch('/:code/toggle', ...)` وزر التشغيل/الإيقاف في الواجهة ما زالا موجودين كما كانا. يحتاج قرارًا صريحًا من المستخدم قبل التنفيذ لأنه قد يمس Runtime Logs (مصدر الإحصائيات) المرتبط بنفس الزر.

## 26. Change Log — تحديث الجلسة الحالية (نقل WAITING لليوم التالي)

### ما تم تنفيذه (القسم 8 من الـSpec)
1. **Backend**: مسار جديد `POST /api/machines/files/transfer-waiting` (مشرف فقط)، Body: `{ workDayId }` (خانة اليوم الحالي).
   - يبحث في الماكينتين (`big`/`small`) وقائمة الانتظار (`queue`) عن كل الملفات بحالة `WAITING` في هذه الخانة فقط.
   - ينقلها لخانة اليوم التالي بتحديث `work_day_id` **على نفس صف `machine_files`** (UPDATE فقط، بدون INSERT) — فلا Duplicate، وتبقى كل بياناته (Thumbnail، تعليق، صوت، File Time، File Status) كما هي تلقائيًا لأنه نفس الصف بنفس الـid.
   - `sort_order` الجديد للملفات المنقولة يوضع بعد آخر ترتيب موجود فعليًا في اليوم التالي (لا يستبدل ترتيب ملفات موجودة هناك مسبقًا).
   - الخميس (`thu`) ينتقل تلقائيًا إلى الجمعة (`fri`) من **الأسبوع التالي** (مع لفّ الأسبوع 4 → 1، لأن الشبكة أسابيع 1-4 ثابتة متكررة وليست تواريخ حقيقية — هذا امتداد منطقي لقرار "استقلال الأيام" من الجلسة السابقة).
   - العملية كاملة داخل DB Transaction واحدة (commit/rollback) لضمان عدم نقل جزء من الملفات فقط عند فشل منتصف الطريق.
   - يُسجَّل Log + Notification بعدد الملفات المنقولة (فقط لو `totalMoved > 0`)، ويُبَث تحديث Socket.io لكل من (big/small/queue) لليوم الحالي (الذي فرغ من ملفاته المنقولة) لكل من يشاهده الآن — نفس نمط البث المستخدم في بقية الملف (`broadcastMachineUpdate`)، لم يُعَد بناء Socket.io.
2. **Frontend**: زر **"نقل الانتظار لليوم التالي"** أعلى قسم قائمة الانتظار في `AdminDash` (يظهر فقط عند وجود `queue`، أي للمشرف فقط أصلاً بنفس منطق العرض الحالي). عند الضغط: `window.confirm` توضيحي، ثم استدعاء الـAPI، ثم Toast بعدد الملفات المنقولة (أو رسالة "لا توجد ملفات" لو صفر)، ثم `onChanged()` لتحديث القائمة الحالية فورًا (الملفات المنقولة تختفي من اليوم الحالي مباشرة).
3. لم يُمَس أي شيء في: منطق حالات الملفات، `selectedFileIds`/Bulk، Drag&Drop، الشكل العام، Login/Sidebar — تمامًا كما يلزم القسم 4 و16 (Locked).

### الاختبارات المنفَّذة
- `node --check` على كل ملفات backend (بدون أخطاء) بعد الإضافة.
- Babel transform كامل على `<script type="text/babel">` في `frontend/index.html` (نجح بدون أخطاء).
- **لم يتم** Live Testing فعلي (لا وصول لسيرفر/DB حي في هذه الجلسة) — يبقى ضروريًا: التأكد عمليًا من نقل ملف WAITING من الخميس/الأسبوع 4 إلى الجمعة/الأسبوع 1، ومن عدم نقل ملفات WORKING/CUTTING/DELIVERED (يجب أن تبقى في مكانها لأن القسم 8 يخص WAITING فقط دون غيرها؛ ملفات العمل الجارية تُدار لاحقًا فقط عبر Cleanup في القسم 9).

### الملفات المعدَّلة في هذه الجلسة
- `backend/src/routes/machines.js` (مسار جديد `POST /files/transfer-waiting` + ثوابت `DAY_ORDER_BACKEND`/`DAY_NAME_AR_BACKEND`)
- `frontend/index.html` (`Api.transferWaiting`، وزر جديد + حالة `transferring` في `AdminDash`)

### Database Changes
لا يوجد. لا Schema جديد ولا Migration جديد في هذه الجلسة — الميزة تستخدم فقط `work_day_id` الموجود مسبقًا في `machine_files`.

### Safe to Deploy
NO — نفس القيد من الجلسات السابقة: لم يُختبَر حيًا بعد على سيرفر/DB فعلية.

### الخطوة التالية المقترحة (بالترتيب من القسم 18)
1. Live Testing لكل ما سبق (استقلال الأيام، إصلاح الإحصائيات الأسبوعية، نقل الانتظار).
2. Cleanup: خيار مسح ملفات أيام الأسبوع مع Confirmation (القسم 9) — الأيام السبعة محددة افتراضيًا، حذف كل الحالات بلا استثناء (بما فيها WORKING/CUTTING) بدون حذف التاريخ الإحصائي (لأن الإحصائيات تُبنى من `machine_runtime_logs`/`operator_sessions`, وليس من `machine_files` مباشرة — فحذف `machine_files` لا يمس هذه الجداول أصلاً، وهذا يسهّل تنفيذ Cleanup بأمان).
3. توزيع الإحصائيات بالساعة + جدول الجلسات التفصيلي (لمطابقة الصورة المرجعية بدقة أكبر، اختياري).
4. Auto-update الإحصائيات عبر Socket.io.

## 25. Change Log — تحديث الجلسة الحالية (جلسة جديدة — إصلاح قيد الإحصائيات)

### ما تم تحليله
- تمت قراءة `HANDOFF.md` كاملًا كأول خطوة (بدون أي تعديل قبل التحليل)، والصورتين المرجعيتين المرسلتين (لوحة الإحصائيات، ولوحة إدارة ملفات الماكينات).
- تم تتبّع القيد الموثّق فعليًا في "Bugs / قيود معروفة" (القسم 21 سابقًا): `statistics.js` (scope=week) كان يعتمد على `work_weeks.start_date/end_date`، وهذان الحقلان أصبحا NULL دائمًا بعد `migration_day_week_independence.sql` (لأن Week 1-4 أصبحت خانات وهمية ثابتة بلا تاريخ حقيقي). النتيجة: أي طلب إحصائيات أسبوعية كان سيفشل أو يرجّع مدى فارغ خاطئ.
- تمت مقارنة هذا بالصورة المرجعية للإحصائيات: التصميم المطلوب فعليًا يعرض **مدى تاريخ حقيقي** ("الأسبوع 17-09-2026 → 13-09-2026") وليس "الأسبوع 1/2/3/4" — ما يؤكد أن الإحصائيات الأسبوعية يجب أن تُبنى على أسبوع تقويمي حقيقي (جمعة→خميس)، مستقلة تمامًا عن شبكة Week1-4 الوهمية الخاصة بالملفات (تلك للملفات فقط، لا علاقة لها بالإحصائيات).

### ما تم تنفيذه
1. **إصلاح `GET /api/statistics/machines` و`/operators` (scope=week)**: تغيير الاعتماد من `weekId` (يشاور على `work_weeks` الوهمية) إلى `weekStart` (تاريخ الجمعة الحقيقي، YYYY-MM-DD). دالة جديدة `resolveWeekStart()` تحسب أقرب جمعة ≤ التاريخ المُرسَل (أو اليوم الحالي افتراضيًا) بدون أي تحويل توقيت. هذا يفصل الإحصائيات تمامًا عن نموذج Week1-4 الخاص بالملفات (لا تعديل على `migration_day_week_independence.sql` ولا على جداول الملفات).
2. **فلتر ماكينة اختياري** (`machineCode=big|small`) على `/statistics/machines` — يطابق طلب القسم 11 "الماكينات: من الماكينات المسجلة فعليًا".
3. **فلتر مشغّل اختياري** (`userId=<id>`) على `/statistics/operators` — يطابق طلب القسم 11 "اختيار مشغل يجب أن يفلتر الإحصائيات الخاصة به"، ويسحب القائمة من `users` الحقيقيين (role='operator') وليس أسماء ثابتة.
4. **الواجهة (`StatisticsPage`)**: استبدال قائمة "الأسبوع 1/2/3/4" المكسورة (كانت تعرض تواريخ NULL) بمتصفّح أسبوع حقيقي (◀ / مدى التاريخ / ▶ / زر "الأسبوع الحالي")، مطابق شكليًا للصورة المرجعية. أُضيفت أيضًا قائمتا فلترة (ماكينة/مشغّل) وأربع بطاقات KPI أعلى الصفحة (إجمالي جلسات المشغلين، متوسط التشغيل اليومي، إجمالي تشغيل الماكينات، أفضل ماكينة) مبنية من نفس البيانات المُرجَعة من الـAPI (لا أرقام Hard-coded)، تقريبًا من تصميم الصورة المرجعية (القسم 12).
5. `Api.getMachineStats`/`getOperatorStats` في `frontend/index.html` عُدِّلا ليرسلا `weekStart`/`machineCode`/`userId` بدل `weekId`.

### لم يُنفَّذ بعد في هذه الجلسة (حفاظًا على الوقت المتاح، بالترتيب من القسم 18)
- توزيع التشغيل اليومي حسب ساعة اليوم (الرسم الثالث في الصورة المرجعية — Distribution by hour) — الـbackend الحالي يرجّع فقط توزيعًا يوميًا (`daily`) وليس بالساعة؛ يحتاج تجميعًا إضافيًا على مستوى الساعة من `machine_runtime_logs` إن رغب المستخدم به لاحقًا.
- جدول "جلسات المشغلين" التفصيلي (اليوم/التاريخ/وقت الدخول/المدة/الماكينة المستخدمة) كما في الصورة المرجعية — الموجود حاليًا ملخّص لكل مشغّل فقط (إجمالي/عدد جلسات)، وليس جدولًا بجلسة لكل صف.
- زر "نقل الانتظار لليوم التالي" (القسم 8).
- خيار Cleanup لمسح ملفات أيام محددة مع Confirmation (القسم 9).
- Auto-update عبر Socket.io لصفحة الإحصائيات تحديدًا (القسم 13) — الإحصائيات حاليًا تُحمَّل عند تغيير الفلاتر فقط، وليس Live عبر Socket.

### الاختبارات المنفَّذة في هذه الجلسة
- `node --check` على كل ملفات الـbackend (بدون أخطاء).
- Babel transform فعلي (`@babel/preset-react`) على كامل محتوى `<script type="text/babel">` في `frontend/index.html` (نجح بدون أخطاء Syntax).
- **لم يتم** Live Testing على سيرفر/قاعدة بيانات فعلية (لا يوجد وصول لبيئة Live في هذه الجلسة) — يبقى هذا مطلوبًا قبل الاعتبار Safe to Deploy.

### الملفات المعدَّلة في هذه الجلسة
- `backend/src/routes/statistics.js` (استبدال منطق `resolveRange` لـscope=week + فلاتر machineCode/userId)
- `frontend/index.html` (`Api.getMachineStats`/`getOperatorStats`، وإعادة بناء جزء التحكم والـKPI في `StatisticsPage`)

### Database Changes
لا يوجد تغيير Schema في هذه الجلسة. لم تُلمَس `migration_day_week_independence.sql` ولا أي migration آخر.

### Safe to Deploy
NO — لم يُختبَر حيًا بعد. باقي القيود من الجلسة السابقة (تنقل الأيام/الأسابيع للملفات) ما زالت أيضًا بحاجة Live Testing كما هو موثّق في القسم "Needs Testing" أعلاه.

### الخطوة التالية المقترحة (بالترتيب من القسم 18)
1. Live Testing لما سبق (استقلال الأيام + إصلاح الإحصائيات الأسبوعية).
2. زر "نقل الانتظار لليوم التالي" (القسم 8).
3. Cleanup مع Confirmation (القسم 9).
4. توزيع الإحصائيات بالساعة + جدول الجلسات التفصيلي لمطابقة الصورة المرجعية بدقة أكبر (إن رغب المستخدم).
5. Auto-update الإحصائيات عبر Socket.io.

## 24. Change Log — تحديث الجلسة الحالية
### استقلال الأيام السبعة + الأسابيع الأربعة (منفَّذ)
- تحويل نموذج البيانات بالكامل من "يوم نشط واحد بتاريخ حقيقي" إلى "28 خانة ثابتة مستقلة (4×7)".
- Header جديد في الواجهة (تابات أيام + Dropdown أسبوع) بدل صفحة إدارة منفصلة، مطابق للصور المرجعية المرسلة من المستخدم.
- قرار مستخدم: البدء بهذه المرحلة تحديدًا أولاً من بين كل الـSpec، بالترتيب المحدد في القسم 18.
- قيد معروف مؤجَّل: الإحصائيات الأسبوعية معطّلة مؤقتًا (انظر "Bugs" أعلاه) لحين مرحلة الإحصائيات.


## 22. أمر إلزامي لأي AI يعمل على المشروع
بعد **كل جلسة تحليل أو تعديل أو إصلاح** يجب تحديث هذا `HANDOFF.md` تلقائيًا، دون انتظار طلب المستخدم.

يجب تسجيل:
1. ما تم تحليله.
2. ما تم تنفيذه.
3. الملفات المعدلة.
4. APIs المعدلة/المضافة.
5. Database changes/migrations.
6. Bugs المكتشفة.
7. سبب كل Bug.
8. الإصلاح.
9. الاختبارات التي تم تنفيذها ونتائجها.
10. ما يحتاج Live Testing.
11. قرارات المستخدم الجديدة.
12. الأشياء التي يجب عدم تغييرها.
13. الخطوة التالية.

لا تحذف المعلومات المهمة السابقة. عند تغيير قرار، أضف التغيير إلى Change Log.
لا تعتبر Feature مكتملة إلا بعد التحقق من الكود أو الاختبار الفعلي.
لا تدّعي Live Testing بدون وصول فعلي للموقع.
إذا لم يتم تعديل شيء، اذكر ذلك صراحة.

**قاعدة العمل: افحص أولًا → اشرح → اقترح أقل تعديل → نفذ فقط بعد الموافقة عند الحاجة → اختبر → حدّث HANDOFF.**

## 23. Change Log
### Phase 31 FINAL Specification
- 7 أيام من الجمعة إلى الخميس.
- كل يوم مستقل.
- كل أسبوع مستقل.
- Big / Small / Waiting لكل يوم.
- WAITING ينتقل لليوم التالي، والخميس إلى جمعة الأسبوع التالي.
- Week 1–4.
- Cleanup مع الأيام محددة افتراضيًا.
- Cleanup يحذف كل الملفات في الأيام المختارة بدون استثناء.
- الإحصائيات أسبوعية وشهرية وتلقائية.
- المشغلون في الفلاتر من Users الحقيقيين.
- الحفاظ على Socket.io.
- File Row / File Status section محمي من التعديل.
- File Time يدخله المشغل يدويًا.
- إزالة Machine Control فقط.
- لا تغيير Database إلا للضرورة.
- لا Redesign.

### User Decision — Cleanup
حذف كل الملفات في الأيام المختارة، بما فيها WORKING وCUTTING، بدون استثناء.

### User Decision — File Section
جزء الملفات كما هو في الصفوف ثابت ومحمي.

### User Decision — Machine Control
الموقع لا يتحكم فعليًا في الماكينات؛ أزرار تشغيل/إيقاف الماكينة غير مطلوبة.
