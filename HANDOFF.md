# HANDOFF.md — نظام إدارة ماكينات الليزر (Tashkeel CNC)

> 🆕🆕 **جلسة الاختبار الحي (هذه الجلسة — أول اختبار حي حقيقي في تاريخ المشروع):**
> نُفِّذ اختبار حي فعلي كامل بحسب طلب صريح من المستخدم (`live-test-prompt.md`) — انظر
> قسم "نتائج الاختبار الحي" في نهاية هذا الملف للتفاصيل الكاملة. **الخلاصة: كل الميزات
> المتراكمة (Work Days/Weeks، Multi-Select، Bulk Move/Delete، Ctrl+V غير مُعاد اختباره
> هذه الجلسة تحديدًا، Notifications، Statistics بقاعدة منتصف الليل، Socket.io/Auto-Sync)
> اجتازت الاختبار الحي بنجاح تام على مستوى الـBackend — لم يُعثَر على أي Bug حقيقي.**
> **قيد بيئي مهم اكتُشِف هذه الجلسة:** لا توجد بيئة متصفح حقيقية متاحة (headless أو غير
> ذلك) في بيئة تنفيذ الـAI هذه، فلم يُختبَر الفرونت بصريًا (React JSX في متصفح فعلي) —
> **لكن** كل منطق الـBackend (HTTP + Socket.io + قاعدة منتصف الليل) اختُبِر حيًا بالكامل
> ضد MariaDB حقيقية وسيرفر Node حقيقي شغّال، ومنطق Auto-Sync الاحتياطي في الفرونت اختُبِر
> حيًا أيضًا (منسوخ حرفيًا من `index.html` وشُغِّل في Node ضد اتصال Socket.io حقيقي مع
> قطع/استعادة سيرفر فعليين — وليس محاكاة).

> هذا الملف هو نقطة التسليم الرسمية بين محادثات الـAI المتعاقبة على هذا المشروع.
> اقرأ **LAST WORKING STATE** ثم **NEXT EXACT ACTION** أولاً، ثم افحص الكود الفعلي قبل أي تعديل.
> إذا تعارض هذا الملف مع الكود الفعلي، **الكود الفعلي هو المرجع**، ثم صحّح هذا الملف.
>
> ⚠️ **تنبيه هام حول هذا الملف (اكتُشِف في جلسة التحديث الحالية):** النسخة السابقة من هذا الملف كانت **متناقضة داخليًا** — قسم "Statistics" التفصيلي (منتصف الملف) كان يوثّق أن Statistics **مبنية بالكامل** (DB+Backend+Frontend)، بينما ملخص المقدمة وجداول `FILES ACTUAL STATE`/`TESTING STATUS` وقسم `LAST WORKING STATE`/`CONTINUATION PROMPT` في آخر الملف كانت **لم تُحدَّث** ولا تزال تقول إن Statistics "غير منجزة إطلاقًا" أو "لم تبدأ". هذا التعارض تم اكتشافه بفحص الكود الفعلي مباشرة (وليس بالاعتماد على أي قسم من هذا الملف وحده) — **الكود الفعلي كان يطابق القسم التفصيلي المبني، لا الملخصات القديمة.** تم الآن توحيد كل أقسام الملف لتعكس نفس الحالة الحقيقية الواحدة. **الدرس لأي جلسة قادمة: لا تكتفِ بقراءة ملخص واحد من هذا الملف — إن بدا أي تعارض بين الأقسام، افحص الكود فعليًا ليكون الفيصل، وثبّت التعارض هنا صراحةً كما حدث الآن.**
>
> 🆕 **الجلسة السابقة أنجزت:** إكمال "قاعدة منتصف الليل" الحقيقية لإحصائيات Statistics، ثم توحيد هذا الملف بالكامل.
>
> 🆕 **الجلسة السابقة أنجزت (بند 7):** حذف `SettingsPage` الديكورية واستبدالها بـStatistics في التنقّل — بطلب صريح من المستخدم بالمتابعة رغم عدم اختبار الميزات الست السابقة حيًا.
>
> 🆕 **هذه الجلسة أنجزت تحديدًا (بند 8 — Socket.io/Auto-Sync، آخر بند في الخطة الأصلية ذات الـ30 مرحلة):** طلب المستخدم توضيح بند 8 فأوضحه: الهدف "لا حاجة لـF5 أبدًا لرؤية التحديثات" + Auto-Sync احتياطي دوري عند انقطاع اتصال Socket.io تحديدًا. فحص شامل لكل نقاط بث Socket.io في الباك إند أظهر أن **كل** endpoints في `machines.js`/`workdays.js`/`events.js` كانت تبث بالفعل (لا نقص)، والفجوة الوحيدة الحقيقية كانت `backend/src/routes/users.js` (إضافة/حذف مستخدم) **لم يكن يبث أي حدث إطلاقًا**. تم إصلاحها (`user:new`/`user:deleted` لغرفة `admins`)، وأُضيف مستمعان مقابلان في الفرونت. كذلك أُضيف **Auto-Sync احتياطي مشروط**: عند `disconnect` يبدأ جلب دوري كل 20 ثانية، يتوقف فور `connect` مع جلب فوري واحد للّحاق بما فات — **هذا ليس Polling دائمًا**، ولا يمس Statistics (بقيت بلا Socket.io كما هي، بقرار سابق متعمَّد لم يطلب المستخدم تغييره). هذا التعديل يخالف جزئيًا بند DO NOT CHANGE القديم ضد أي Polling — **مخالفة مقصودة وموثَّقة بطلب صريح من المستخدم**، مُحدَّثة في قسم DO NOT CHANGE. **تحقّق Babel نجح على الفرونت، و`node --check` نجح على `users.js`. لم يُختبَر حيًا/بصريًا بعد** (لا سيرفر ولا متصفح حقيقي — القيد المتكرر في كل الجلسات). **هذا يُغلِق آخر بند من الخطة الأصلية على مستوى الكود بالكامل — لم يتبقَّ أي بند "غير منجز" من REMAINING TASKS، فقط "غير مختبَر حيًا".**

---

## PROJECT OVERVIEW

نظام كامل (Backend + Frontend + Database) لإدارة إنتاج ماكينتي ليزر/CNC (كبيرة وصغيرة) + قائمة انتظار مخفية عن المشغّل، بصلاحيات (Root Admin / Admin / Operator)، رفع ملفات حقيقي (R2 أو قرص محلي)، صور معاينة، تعليقات كتابية وصوتية، ونقل/إعادة ترتيب بالسحب والإفلات، مع تحديث لحظي عبر Socket.io.

المطلوب حاليًا (بحسب طلب المستخدم الأصلي، خطة من 30 مرحلة): Branding بشعار الشركة، نظام أيام عمل ستة + أسابيع بتواريخ، Multi-Select/Bulk Actions محمية بالصلاحيات، نظام Ctrl+V (القرار النهائي: لملف واحد فقط)، نظام Notifications متطور، صفحة Statistics (أسبوعية/شهرية/تشغيلين) مع قاعدة "منتصف الليل"، وتحسين Socket.io/Auto-Sync.

**الحالة الحقيقية المؤكَّدة الآن (بعد فحص الكود فعليًا):**
- ✅ منجز فعليًا (على مستوى الكود؛ **لا شيء منها مختبَر حيًا/بصريًا بعد** — انظر التنبيه أعلاه): **Branding** (شعار + ألوان)، **طبقة DB لأيام/أسابيع العمل**، **API كامل لأيام/أسابيع العمل**، **واجهة Admin لأيام/أسابيع العمل (UI)**، **Multi-Select** (state + واجهة تحديد)، **Bulk Move / Bulk Delete** (endpoints خلفية + أزرار فعلية في الواجهة)، **Ctrl+V — COMPLETED نهائيًا** (لصق لملف واحد فقط، مطابق للمواصفة النهائية الثابتة)، **Notifications المتطور** (تصنيف + فلترة + صفحة كاملة)، 🆕 **Statistics بكل أجزائها** — Runtime فعلي للماكينات + جلسات المشغّلين (حضور/انصراف) + تقارير أسبوعية/شهرية + **قاعدة منتصف الليل الحقيقية (تقسيم فعلي بالثانية، أُكمِلت هذه الجلسة)**.
- ✅ 🆕 منجز أيضًا (على مستوى الكود، غير مختبَر حيًا): **حذف/استبدال Settings** — `SettingsPage` الديكورية حُذِفت بالكامل، Statistics هي البديل الفعلي الوحيد الآن.
- ✅ 🆕 منجز أيضًا (على مستوى الكود، غير مختبَر حيًا): **تحسينات Socket.io/Auto-Sync** — بث `user:new`/`user:deleted` (كانت الفجوة الوحيدة الحقيقية) + Auto-Sync احتياطي مشروط بانقطاع الاتصال.
- ❌ غير منجز على مستوى الكود من الخطة الأصلية: **لا شيء** — كل بنود الخطة الأصلية ذات الـ30 مرحلة مكتملة الآن على مستوى الكود. **المتبقي بالكامل هو الاختبار الحي/البصري** لكل الميزات (لم يحدث لأي منها حتى الآن).
- ✅ **القرار النهائي الثابت بخصوص Ctrl+V (لم يعد قيد النقاش):** لا يوجد ولن يوجد نظام لصق جماعي (Bulk Paste) لعدة ملفات دفعة واحدة. `selectedFileIds` مخصَّص حصريًا لـBulk Actions (حذف/نقل)، و`pasteTargetId` مخصَّص حصريًا لملف واحد يستقبل Ctrl+V. النظام الحالي (`pasteTargetId`) هو **التنفيذ النهائي والكامل** لميزة Ctrl+V في هذا المشروع. 🆕 **تمت مراجعته وتأكيده هذه الجلسة (بدون أي تعديل كود) مطابقًا حرفيًا لهذه المواصفة — انظر قسم "CTRL+V FINAL VERIFICATION" أدناه.**

---

## ARCHITECTURE

```
laser-system-main/
├── backend/  (Node.js + Express + MySQL(mysql2) + Socket.io + JWT + Multer + R2/S3)
│   └── src/
│       ├── config/db.js        pool اتصال MySQL
│       ├── config/r2.js        رفع/قراءة/حذف من Cloudflare R2
│       ├── middleware/auth.js  authenticate / requireAdmin / requireRoot
│       ├── middleware/upload.js multer (ملفات + صور + صوت)
│       ├── routes/auth.js, users.js, machines.js (761 سطر), logs.js, notifications.js, workdays.js (196 سطر)
│       ├── utils/events.js     addLog / addNotification
│       ├── utils/time.js       timeToSeconds / secondsToTime
│       ├── utils/presence.js   تتبع Online/Offline عبر Socket.io
│       └── server.js           نقطة التشغيل + Socket.io + كل الـRoutes (يسجّل /api/work أيضًا)
├── frontend/
│   ├── index.html   (1710 سطر) — React كامل عبر CDN + Babel-standalone (بدون build step)، كل شيء (State/API/UI) في ملف واحد
│   ├── config.js    API_BASE
│   └── assets/       logo.png, logo-mark.png
├── database.sql (7 جداول: users, machines, machine_files, work_weeks, work_days, logs, notifications)
├── migration_workdays.sql, migration_queue.sql, migration_comments.sql, migration_thumbnail.sql
└── HANDOFF.md  ← هذا الملف
```

**نقاط معمارية مهمة يجب معرفتها قبل أي تعديل:**
- الـFrontend **بدون build step إطلاقًا** — React JSX يُترجَم في المتصفح لحظيًا عبر `babel-standalone`. تم التحقق أن الملف الحالي **يُجمَّع بنجاح عبر `@babel/core` (preset-react + preset-env)** بدون أخطاء نحوية (فُحص في هذه الجلسة).
- كل الألوان **مكتوبة inline كـhex codes** داخل `style={{...}}` — لا يوجد نظام CSS variables/theme مركزي.
- 🆕 الـBackend **يحتوي الآن فعليًا** على "Runtime فعلي للماكينة" (`machine_runtime_logs` + `machines.running_started_at`)، "Sessions للمشغلين" (`operator_sessions`)، و"قاعدة منتصف الليل" الحقيقية (تقسيم بالثانية عبر `splitIntervalByDay`، وليس مجرد نسب لليوم الأول) — كل هذا **على مستوى الكود فقط، غير مختبَر حيًا بعد**. انظر قسم Statistics أدناه للتفاصيل الكاملة.
- `machines.js` هو أكبر ملف منطق (761 سطر، 19 endpoint) ويحتوي كل عمليات CRUD للملفات + النقل + التعليقات + الصور — **كل عملياته على ملف واحد فقط، لا يوجد أي endpoint يقبل مصفوفة IDs (bulk)** (تم التحقق بفحص كل الـrouter.* في الملف).
- الجدول `machine_files` يحمل عمود `work_day_id` (وسم/Tag تاريخي فقط، `ON DELETE SET NULL`، ليس Snapshot).
- Socket.io: كل التحديثات تُبَث عبر `io.emit(...)` أو لغرفة `admins` فقط لقائمة الانتظار. أحداث `workday:update` و`workweek:new` مضافة بنفس النمط.
- `backend/src/routes/workdays.js` (196 سطر) مسجَّل في `server.js` على `/api/work/*` — **لا يوجد أي كود Frontend يستهلكه** (تم التحقق: بحث شامل عن `api/work`, `workday`, `workweek`, `work_day` في `index.html` = صفر نتائج).

---

## CURRENT PROJECT STATE (مُصحَّح بعد فحص الكود)

### ✅ Branding / Logo — COMPLETED (تم التحقق من الكود مباشرة)
- ثابت `LOGO_B64` (صورة Base64 مضمَّنة) موجود في `index.html` ومُستخدَم في مكانين فعليين: شاشة الدخول (`<img src={LOGO_B64}.../>` بارتفاع 44px) والـTopbar (26px).
- اللون البرتقالي: **50 موضعًا فعليًا** يستخدمون `#e06a24` (وليس 52 كما زعم الملف السابق)، وصفر مواضع متبقية لـ`#f97316` القديم.
- لون Root Admin المميّز (`#8e3537` / `#c2483d`) موجود فعليًا في شارتين (قائمة المستخدمين + Topbar).
- Favicon موجود (`<link rel="icon" href="./assets/logo-mark.png">`) — **ملاحظة:** هذا مسار ملف نسبي (وليس Base64)، بعكس اللوجو الرئيسي المضمَّن كـBase64 لضمان الظهور بغض النظر عن الاستضافة؛ الفافيكون وحده يعتمد على نجاح نشر مجلد `assets/` — قد لا يظهر إذا فشل نشر الأصول الثابتة.
- **الحالة:** موجود ويعمل نحويًا (اجتاز فحص Babel في هذه الجلسة). **لم يُختبَر بصريًا في متصفح حقيقي بعد** (لا بيئة متصفح متاحة لأي جلسة AI سحابية حتى الآن) — يُنصَح المستخدم بمراجعته بصريًا.

### ✅ Work Days (DB) — COMPLETED
- جدول `work_days` موجود في `database.sql` و`migration_workdays.sql` بنفس البنية (`id, work_date UNIQUE, day_name ENUM(sat,sun,mon,tue,wed,thu), week_id, is_active, created_at`).
- عمود `work_day_id` على `machine_files` موجود (`ON DELETE SET NULL`) + index.
- **الحالة:** الكود موجود ومطابق للتوثيق. **لم تُعَد إعادة اختباره حيًا (MariaDB) في هذه الجلسة** — الاختبار الموثَّق سابقًا (تحميل schema، ترقية قاعدة حية، اختبار Tag-not-Snapshot، اختبار UNIQUE) كان في جلسة سابقة؛ لم يُعَد تكراره هنا لأن هذه جلسة تدقيق فقط (بدون تنفيذ). يُنصَح بإعادة اختبار حي سريع في أول جلسة تنفيذ قادمة قبل البناء فوقه، فقط للتأكد.

### ✅ Work Weeks (DB) — COMPLETED
- جدول `work_weeks` موجود (`id, week_number, start_date UNIQUE, end_date, created_at`) في `database.sql` و`migration_workdays.sql`. مطابق للتوثيق السابق.

### ✅ Work Days API — COMPLETED (كود موجود ومطابق تمامًا لما هو موثَّق)
`backend/src/routes/workdays.js` (196 سطر) — تم فحصه سطرًا بسطر ويطابق الوصف:
- `GET /api/work/days`, `GET /api/work/days/active` (قراءة، أي مستخدم مسجّل).
- `POST /api/work/days` (Admin) — يرفض الجمعة، يربط بالأسبوع تلقائيًا، يُصفّر أي يوم نشط سابق (Transaction)، upsert آمن.
- `PATCH /api/work/days/:id/close` (Admin) — Tag فقط، لا حذف.
- `GET /api/work/weeks`, `GET /api/work/weeks/:id`, `POST /api/work/weeks` (Admin, يرفض تكرار start_date بـ409).
- مسجَّل في `server.js`: `app.use('/api/work', require('./routes/workdays')(io));` ✅ مؤكَّد.
- يستخدم `authenticate`/`requireAdmin` الموجودين (لا نظام صلاحيات جديد) ✅ مؤكَّد.
- يبث `workday:update` و`workweek:new` عبر `io.emit` ✅ مؤكَّد.
- **الحالة:** الكود سليم نحويًا (`node --check` نجح في هذه الجلسة). الاختبار الحي الموثَّق سابقًا (curl + JWT حقيقي + عميل socket.io-client) لم يُعَد تكراره في هذه الجلسة التدقيقية.

### ✅ Work Days UI — COMPLETED 🆕 (هذه الجلسة)
- تمت إضافة `WorkDaysPage` كمكوّن React جديد في `frontend/index.html` (بين `LogsPage` و`SettingsPage`)، وربطه بعنصر تنقل جديد `{id:"workdays", l:"أيام العمل", n:"cal"}` في `navAdmin` (بين "السجل" و"الإعدادات") — **متاح للمشرفين فقط**، بنفس نمط صفحات `users`/`logs`/`settings`.
- المحتوى: بطاقة "اليوم النشط الآن" (بعرض اسم اليوم بالعربية + التاريخ، أو رسالة "لا يوجد يوم نشط")، مع حقل تاريخ + زر "بدء يوم جديد" وزر "إغلاق اليوم" (Admin فقط) — يستهلكان `Api.startWorkDay(date)` و`Api.closeWorkDay(id)`.
- جدول "سجل أيام العمل" (من `Api.getWorkDays()`) بعرض التاريخ/اسم اليوم/رقم الأسبوع/الحالة (نشط/مغلق) وزر إغلاق لكل يوم نشط.
- قسم "أسابيع العمل" (من `Api.getWorkWeeks()`) مع نموذج "أسبوع جديد" (رقم الأسبوع، تاريخ البداية، تاريخ النهاية) يستهلك `Api.createWorkWeek(...)`.
- أُضيفت 7 دوال جديدة لكائن `Api` تطابق مسارات `workdays.js` حرفيًا: `getWorkDays`, `getActiveWorkDay`, `startWorkDay`, `closeWorkDay`, `getWorkWeeks`, `getWorkWeek`, `createWorkWeek`.
- أُضيف أيقونة `cal` جديدة لمكوّن `I` (لم تكن موجودة سابقًا).
- في `App`: أُضيف state جديد (`workDays`, `workWeeks`, `activeDay`) ودالة `refreshWorkDays` (تُستدعى بعد تسجيل الدخول إن كان `role==="admin"`، بنفس نمط `refreshUsers`)، وأُضيف مستمعا Socket.io جديدان `workday:update` و`workweek:new` يحدّثان الـstate مباشرة بدل أي polling — **لا يوجد أي polling جديد**.
- **لم يُلمَس `pasteTargetId` إطلاقًا** (تحقَّق: نفس عدد المطابقات 8 قبل وبعد التعديل) — منفصل تمامًا كما هو مطلوب.
- **طريقة الاختبار المُنفَّذة فعليًا (بدون متصفح، بيئة الجلسة الحالية):**
  - تجميع فعلي عبر `@babel/core` (`transformSync` بـ`preset-react` + `preset-env`) لكامل سكربت الـFrontend بعد التعديل — **نجح بدون أي خطأ syntax**.
  - `node --check` على كل ملفات الـBackend (لم تتغيّر) — نجح.
  - بحث نصي مباشر يؤكّد أن `getWorkDays/getActiveWorkDay/startWorkDay/closeWorkDay/getWorkWeeks/createWorkWeek` موجودة في `index.html` (10 مطابقات) وأن `selectedFileIds` لا يزال صفر نتائج (لم يُبدأ Multi-Select، كما هو متوقّع).
  - **لم يُختبَر بصريًا في متصفح حقيقي، ولم يُختبَر بتشغيل حي (MariaDB + Node + Socket.io) في هذه الجلسة تحديدًا** — لا بيئة متصفح ولا شبكة/خدمات متاحة لهذه الجلسة. هذا مطلوب من المستخدم كخطوة تالية فورية: فتح الواجهة، تسجيل الدخول كمشرف، الضغط على "أيام العمل"، تجربة بدء يوم/إغلاقه/إنشاء أسبوع، والتأكد من التحديث اللحظي عبر Socket.io بين أكثر من نافذة/جهاز.

### ✅ Multi-Select — مكتمل بالجزء المطلوب منه (state + واجهة تحديد)
- `selectedFileIds` كـstate داخل `AdminTable` (مصفوفة IDs)، **منفصل تمامًا عن `pasteTargetId`** (لا تقاطع قراءة/كتابة بينهما، تحقَّق مجددًا هذه الجلسة).
- `useEffect` يُنظّف `selectedFileIds` تلقائيًا من أي id لم يعد موجودًا في `localFiles`.
- عمود checkbox لكل صف + checkbox "تحديد الكل" (indeterminate عند تحديد جزئي).
- موجود فقط داخل `AdminTable` (ضمن `AdminDash`)، وليس في `OpTable` — محمي فعليًا بكونه ضمن مسار المشرف فقط في الواجهة (نفس نمط حماية بقية صفحات الإدارة).

### ✅ Bulk Move / Bulk Delete — بُنيَت بالكامل 🆕 (هذه الجلسة) — Backend + Frontend، غير مختبَرة حيًا بعد
**Backend (`backend/src/routes/machines.js`):**
- `PATCH /api/machines/files/bulk-move` (`requireAdmin`) — body: `{fileIds:[], targetMachineCode}`. يُنفَّذ لكل id **تباعًا** (وليس بالتوازي، للحفاظ على `sort_order` صحيح عبر `maxOrder` المتغيّر). يتخطى (`skippedIds`) أي id غير موجود أو موجود مسبقًا في الهدف، بدل فشل العملية كاملة. بثّ واحد فقط لكل ماكينة متأثرة (مصدر/أهداف)، وسجل واحد مُجمَّع (بحد أقصى 5 أسماء + "و+N أخرى").
- `DELETE /api/machines/files/bulk-delete` (`requireAdmin`) — body: `{fileIds:[], machineLabel}`. نفس منطق الحذف الفردي حرفيًا لكل ملف (حذف الملف الفعلي + الصورة + الصوت من R2/القرص، إعادة الوقت لماكينته)، مع بثّ واحد لكل ماكينة متأثرة وسجل واحد مُجمَّع.
- **حد أقصى 200 id لكل عملية واحدة** (`MAX_BULK_IDS`) — رفض بـ400 إن تجاوز أو كانت المصفوفة فارغة.
- **⚠️ ملاحظة ترتيب حرجة موثَّقة في الكود نفسه:** المساران `bulk-move`/`bulk-delete` **يجب أن يبقيا مُعرَّفين قبل** المسارات العامة `/files/:fileId` (نقل/حذف فردي) — وإلا سيلتقطهما Express كقيمة `fileId` حرفية ("bulk-move"). تم وضعهما في الترتيب الصحيح (قبل قسم "حذف ملف" الفردي).
- **لا نظام صلاحيات جديد** — استُخدم `requireAdmin` الموجود فقط (بند DO NOT CHANGE).
- **`node --check` نجح على `machines.js` وكل ملفات الـBackend** (هذه الجلسة).

**Frontend (`frontend/index.html`):**
- دالتان جديدتان في `Api`: `bulkMoveFiles(fileIds,targetMachineCode)` و`bulkDeleteFiles(fileIds,machineLabel)`.
- داخل `AdminTable`: دالتا `bulkMove(targetMachineCode)` و`bulkDelete()` (تستخدمان `wrap()` الموجودة لعرض busy/أخطاء بنفس نمط بقية الأزرار)، تعتمدان حصريًا على `selectedFileIds` الموجود، وتُفرِّغان التحديد (`clearSelection()`) بعد نجاح العملية.
- شريط التحديد الآن يحتوي فعليًا: قائمة منسدلة "نقل إلى..." (تعرض الماكينتين/القائمة الأخرى غير الحالية عبر `MACHINE_LABELS` جديد ثابت)، وزر "حذف المحدد" (بتأكيد `confirm()` قبل التنفيذ، بنفس نمط الحذف الفردي)، وزر "إلغاء التحديد" كما كان.
- **بخصوص Ctrl+V وBulk (قرار نهائي ثابت، غير مفتوح للنقاش):** لا يوجد ولن يوجد نظام Ctrl+V يعمل على `selectedFileIds` (لا لصق لعدة ملفات دفعة واحدة). `selectedFileIds` مخصَّص حصريًا لـBulk Actions (نقل/حذف)، وCtrl+V يعمل حصريًا عبر `pasteTargetId` لملف واحد — انظر قسم Ctrl+V أعلاه (COMPLETED).
- **بحث نصي يؤكّد:** `pasteTargetId` لا يزال بنفس 8 مطابقات وظيفية — لم يُمَس بأي شكل عند إضافة أزرار Bulk.

### ✅ Ctrl+V (نظام الملف الواحد) — COMPLETED (القرار النهائي الثابت، تم التحقق منه مجددًا)
- **ما هو موجود فعليًا ويعمل الآن:** `pasteTargetId` (state واحد لملف واحد) — عند التركيز (`onFocus`) على صف ملف، يصبح هو "هدف اللصق"؛ عند `Ctrl+V` بصورة، يُستدعى `Api.uploadThumbnail` لرفعها كصورة معاينة لهذا الملف تحديدًا.
- **البَگ الذي كان موجودًا (تم إصلاحه هذه الجلسة):** `AdminTable` يُركَّب 3 مرات متزامنة (كبيرة/صغيرة/قائمة الانتظار)، وكل نسخة كان عندها `pasteTargetId` مستقل + `window.addEventListener("paste",...)` مستقل. لو صار عندنا أكثر من نسخة نشِطة (Target) في نفس الوقت — بسبب التركيز على صف في جدول ثم صف في جدول آخر، أو بسبب أن الضغط على الـCheckbox (Multi-Select) كان يُطلِق `onFocus` الخاص بالصف عبر الـBubbling ويُغيّر `pasteTargetId` بدون قصد — كان ينتج أكثر من Listener فعّال في آن واحد، فيلصق Ctrl+V واحد الصورة في أكثر من ملف (أحيانًا في جداول مختلفة).
- **تفاصيل الإصلاح:**
  - **الملفات المعدَّلة:** `frontend/index.html` فقط.
  - **الـFunctions/المواضع المعدَّلة داخل `AdminTable`:** تعريف الـstate/الـrefs في أعلى الدالة، الـ`useEffect` الخاص بـ`window.addEventListener("paste",...)`، `onFocus` الخاص بالصف (`<tr>`)، و`onFocus` الخاص بالـ`<input type="checkbox">` في كل صف. إضافة متغيّر عام واحد على مستوى الملف (خارج أي Component): `globalPasteTargetOwner`.
  - **حل Duplicate Paste:** إضافة `isPastingRef` (useRef) كقفل — أي Paste يصل أثناء رفع سابق قيد التنفيذ يُتجاهَل فورًا (`if(isPastingRef.current) return;`)، ويُحرَّر القفل دائمًا في `finally` (نجاح أو فشل).
  - **تثبيت `pasteTargetId`:** عند بداية الـ`useEffect` (أي عند تفعيل هدف اللصق) يُؤخَذ `const targetId=pasteTargetId;` وتُستخدَم `targetId` فقط طوال عملية الرفع بدل الاعتماد على المتغيّر المتحرّك — فحتى لو حدث Re-render أثناء الرفع، الصورة تذهب للهدف الأصلي فقط.
  - **فصل `selectedFileIds` عن `pasteTargetId` (تعزيز):** أُضيف `onFocus={e=>e.stopPropagation()}` على كل `<input type="checkbox">` في الصف، لمنع وصول حدث الـFocus (Bubbling) إلى `onFocus` الخاص بالصف — الآن الضغط على الـCheckbox (تحديد فردي أو ضمن Multi-Select) **لا يُغيّر `pasteTargetId` إطلاقًا**. `selectedFileIds` نفسه لم يُقرأ ولم يُكتَب من أي كود خاص بالـPaste (كما كان سابقًا)، ولا يتغيّر بعد نجاح/فشل Paste.
  - **منع تشغيل أكثر من Listener فعّال في آن واحد (السبب الجذري):** أُضيف `globalPasteTargetOwner` (متغيّر عام واحد خارج الـComponent) يحمل دالة تصفير (`clearMyPasteTarget`, عبر `useCallback`) خاصة بأي نسخة `AdminTable` تملك حاليًا Target فعّال. عند `onFocus` على أي صف، إن كانت هناك نسخة أخرى صاحبة Target فعّال، يتم استدعاء دالة تصفيرها أولًا (فتُغلِق هي Listener الخاص بها عبر cleanup الخاص بـ`useEffect`) قبل تفعيل الهدف الجديد. كما يُصفَّر المرجع عند نجاح/فشل الـPaste وعند إلغاء تركيب (`unmount`) أي نسخة، لمنع أي مرجع معلّق (Stale).
- **نتيجة الاختبارات (تحليل ثابت للكود + Syntax Check، بدون تشغيل حي في متصفح — لم يتوفر Runtime فعلي في هذه الجلسة):**
  - تم فحص الكود يدويًا مقابل كل الحالات الـ15 المطلوبة (اختيار A ثم Paste → A فقط؛ اختيار B ثم Paste → B فقط؛ تحديد A+B+C ثم اختيار B كـTarget ثم Paste → B فقط ولا يتأثر A/C ولا `selectedFileIds`؛ Paste متكرر بسرعة → رفع واحد فقط بفضل `isPastingRef`؛ Re-render أثناء الرفع → الصورة للـTarget الأصلي بفضل `targetId` الثابت؛ فتح/إغلاق Modal → لا تأثير لأن الـLightbox يُركَّب على مستوى `App` وليس داخل شجرة `AdminTable`؛ Checkbox/Select All → لا يغيّر `pasteTargetId` — Select All أصلًا في `<thead>` خارج أي `<tr>` له `onFocus`، والـCheckbox الآن يوقف الـBubbling صراحة؛ تغيير الـTarget لملف آخر ثم Paste → للملف الجديد فقط؛ فشل الرفع → القفل يُحرَّر عبر `finally` فيمكن إعادة المحاولة؛ Listener واحد فقط في كل لحظة عبر كل نسخ `AdminTable` بفضل `globalPasteTargetOwner`؛ لا Duplicate API Request بفضل نفس القفل).
  - **Syntax/Build:** تم استخراج كتلة `<script type="text/babel">` كاملةً (بعد التعديل) وتحويلها فعليًا عبر `esbuild` (JSX loader) محليًا بدون أخطاء — **هذه أول جلسة يتم فيها تشغيل تحويل JSX حقيقي (لا مجرد فحص توازن أقواس) بفضل توفر `esbuild` ضمن حزم Node المثبَّتة مسبقًا في هذه البيئة**، رغم عدم توفر شبكة لتثبيت Babel نفسه.
  - **لم يُختبَر:** تشغيل حي في متصفح فعلي (لا سيرفر ولا DB متاحين في هذه الجلسة) — هذا تحذير صريح كالعادة.
- **مشكلة متبقية إن وُجدت:** لا شيء معروف على مستوى الكود الثابت. التوصية: اختبار حي في متصفح لتأكيد الحالات الـ15 فعليًا، خصوصًا سيناريو التركيز على جدولين مختلفين (كبيرة/صغيرة/قائمة الانتظار) بالتتابع ثم Ctrl+V واحدة.
- **القرار النهائي الثابت (لم يعد بندًا مفتوحًا):** لا يوجد نظام Ctrl+V مرتبط بـ`selectedFileIds` (لا يوجد لصق لكل الملفات المحددة دفعة واحدة)، ولن يُبنى مستقبلاً. `selectedFileIds` = Bulk Actions فقط، `pasteTargetId` = ملف واحد فقط يستقبل Ctrl+V. النظام الحالي (`pasteTargetId`) هو التنفيذ الكامل والنهائي لميزة Ctrl+V. لم تُنفَّذ أي Feature أخرى (Notifications، Statistics، Work Days/Weeks، Bulk، DB Schema) في هذه الجلسة كما طُلب صراحةً.

#### 🔎 CTRL+V FINAL VERIFICATION (هذه الجلسة) — Status: COMPLETED, NO CODE CHANGES
- **الطلب:** مراجعة حالة Ctrl+V الحالية فقط للتأكد من مطابقتها للمواصفة النهائية الثابتة (`selectedFileIds`=A+B+C كلها لـBulk، `pasteTargetId`=B فقط لـCtrl+V)، **بدون** إعادة تنفيذ أو إعادة تصميم، وبدون تعديل الكود إلا إذا وُجد خطأ فعلي.
- **النتيجة: لم يُعثَر على أي خطأ. لم يُعدَّل أي سطر كود.** فُحصت كل النقاط المطلوبة تحديدًا (Verified by direct code reading سطرًا بسطر في `frontend/index.html`، بالإضافة إلى فحص توازن الأقواس لكامل سكربت الـFrontend بعد التأكد من عدم لمسه — متوازن تمامًا):
  1. **لا يوجد Duplicate Paste Listener:** `useEffect` واحد يضيف `window.addEventListener("paste",...)` فقط، ويُنفَّذ فقط عندما تكون `pasteTargetId` لهذه النسخة غير `null`؛ `globalPasteTargetOwner` يضمن ألا يكون هناك أكثر من نسخة `AdminTable` (كبيرة/صغيرة/قائمة الانتظار) بها `pasteTargetId` فعّال في نفس اللحظة (تصفير أي مالك سابق قبل تفعيل مالك جديد عند `onFocus`). **PASS**
  2. **لا يوجد Duplicate Upload:** `isPastingRef` (useRef) يُفحَص كأول سطر في الـhandler (`if(isPastingRef.current) return;`)، ويُحرَّر دائمًا في `finally` (نجاح أو فشل)، وهو نفس المرجع عبر كل الـRenders لنفس النسخة. **PASS**
  3. **`isPastingRef` يعمل بشكل صحيح:** يُقفَل قبل بدء الرفع (`isPastingRef.current=true`) ويُحرَّر في `finally` بعد `setPasteTargetId(null)` وتصفير `globalPasteTargetOwner`. **PASS**
  4. **`targetId` ثابت أثناء عملية Upload:** يُؤخَذ `const targetId=pasteTargetId;` داخل الـ`useEffect` نفسه قبل أي `await`، وتُستخدَم `targetId` (وليس `pasteTargetId` المتغيّر) حصريًا طوال دورة الرفع (`Api.uploadThumbnail(targetId,...)`). **PASS**
  5. **Checkbox وSelect All لا يغيّران `pasteTargetId`:** كل `<input type="checkbox">` في صف الملف يحمل `onFocus={e=>e.stopPropagation()}` فيمنع وصول حدث الـFocus (عبر الـBubbling) إلى `onFocus` الخاص بالـ`<tr>`. checkbox "تحديد الكل" موجود في `<thead>` منفصل تمامًا عن أي `<tr>` له `onFocus` أصلاً. **PASS**
  6. **`selectedFileIds` لا تتغيّر بعد Paste:** غير مقروءة وغير مكتوبة من أي كود داخل نظام الـPaste (الـ`useEffect`، الـhandler، أو الـfinally) في أي اتجاه. **PASS**
  7. **Re-render لا يغيّر الـTarget:** حتى لو أعاد الـ`useEffect` تسجيل الـlistener بسبب تغيّر باقي الاعتماديات (`localFiles`, `onChanged`, ...)، القيمة المُعاد حسابها لـ`targetId` تبقى نفس `pasteTargetId` الحالي طالما لم يتغيّر فعليًا (أي لم يحدث `onFocus` جديد) — لا يوجد أي مسار كود يُغيّر `pasteTargetId` بسبب Re-render وحده. **PASS**
  8. **Big Machine وSmall Machine وQueue لا تسبب أكثر من Paste Target فعّال:** الثلاث نسخ من `AdminTable` تتشارك نفس `globalPasteTargetOwner` (متغيّر عام واحد خارج الـComponent) — فبنيويًا لا يمكن لأكثر من نسخة أن تملك Target/Listener فعّال في آن واحد. **PASS**
- **جميع النقاط الثمانية المطلوبة: PASS.** الحالة الحالية مطابقة تمامًا للقرار النهائي الثابت.
- **اعتبار الحالة:** **Ctrl+V = COMPLETED.** لا حاجة لأي تعديل كود إضافي على هذه الميزة ما لم يظهر خطأ فعلي جديد مستقبلاً.

#### 🔎 CTRL+V REVIEW (جلسة منفصلة لاحقة) — Status: VERIFIED, NO CODE CHANGES
- **الطلب:** مراجعة/تدقيق الإصلاح الموجود بالفعل (أعلاه) للتأكد من التزامه بقاعدة الفصل بين `selectedFileIds` و`pasteTargetId`، **بدون** تعديل أي كود، وبدون الانتقال لأي Feature أخرى.
- **النتيجة: لم يُعثَر على أي مشكلة إضافية. لم يُعدَّل أي سطر كود في هذه الجلسة** — تم فقط الفحص والاختبار الثابت.
- **ما تم فحصه/التأكد منه بمراجعة الكود سطرًا بسطر (كل هذا "Verified by code review"، ليس Live Browser Test):**
  - يوجد `useEffect` واحد فقط لـ`window.addEventListener("paste",...)` لكل نسخة `AdminTable`، مع `return ()=>window.removeEventListener(...)` صحيح — لا Listener مكرر، و`OpTable` (واجهة المشغّل) لا تحتوي أي كود Paste إطلاقًا فلا تعارض معها.
  - `targetId` يُثبَّت (`const targetId=pasteTargetId;`) داخل الـ`useEffect` نفسه قبل أي `await`، ويُستخدَم حصريًا طوال دورة الرفع.
  - `isPastingRef.current` يُفحَص كأول سطر في الـhandler (`if(isPastingRef.current) return;`) قبل أي منطق آخر، ويُحرَّر في `finally` في كل الحالات (نجاح/فشل عبر `catch`).
  - `globalPasteTargetOwner` (متغيّر وحيد خارج الـComponent) يُصفِّر أي نسخة `AdminTable` أخرى قبل تفعيل هدف جديد (`onFocus` الخاص بالصف)، ويُصفَّر بدوره عند نجاح/فشل الـPaste وعند `unmount` — فلا يمكن لأكثر من نسخة (كبيرة/صغيرة/قائمة الانتظار) أن تملك Target/Listener فعّال في آن واحد.
  - كل `<input type="checkbox">` في صف الملف يحمل `onFocus={e=>e.stopPropagation()}` فعليًا، فلا يصل حدث الـFocus (Bubbling) إلى `onFocus` الخاص بالـ`<tr>` — التحديد الفردي أو ضمن Multi-Select **لا يغيّر `pasteTargetId`**.
  - Checkbox "تحديد الكل" (Select All) موجود في `<thead>` منفصل تمامًا عن أي `<tr>` له `onFocus` — لا يمكنه بنيويًا الوصول لمنطق الـPaste أصلًا (لا حاجة لأي حماية إضافية).
  - نافذة المعاينة (`Lightbox`/Modal) تُركَّب على مستوى `App` (خارج شجرة `AdminDash`/`AdminTable` تمامًا) — فتحها/إغلاقها لا يمكن أن يُطلِق `onFocus` أي صف بأي شكل.
  - `selectedFileIds` غير مقروء وغير مكتوب من أي كود داخل نظام الـPaste (الـ`useEffect`، الـhandler، أو الـfinally) في أي اتجاه — يبقى كما هو قبل/أثناء/بعد أي عملية لصق، ناجحة أو فاشلة.
- **اختبار آلي (Simulation) — وليس Live Browser Test:** كُتِبت محاكاة Node.js منفصلة (`/home/claude/sim_test.js`، خارج المشروع) تُعيد تنفيذ نفس خوارزمية الكود الفعلي حرفيًا (نفس تسلسل: تثبيت الـTarget، قفل `isPastingRef`، منطق `globalPasteTargetOwner` لتصفير/تعيين المالك، إرفاق/فك الـListener عند تغيّر `pasteTargetId`) لمحاكاة الحالات التسع المطلوبة:
  1. تركيز A ثم Paste → رفع لـA فقط. **PASS**
  2. تحديد A+B+C (Checkbox)، B هو Target، Paste → رفع لـB فقط. **PASS**
  3. `selectedFileIds` تبقى A+B+C بعد الـPaste. **PASS**
  4. الضغط على Checkbox لا يغيّر `pasteTargetId`. **PASS**
  5. Select All لا يغيّر `pasteTargetId`. **PASS**
  6. Ctrl+V ثلاث مرات بسرعة (بدون انتظار الأولى) → رفع واحد فقط. **PASS**
  7. تغيير Target من A إلى B ثم Paste → لـB فقط. **PASS**
  8. فتح/إغلاق Modal (لا يُطلِق أي Focus على أي صف بنيويًا) ثم Paste → للـTarget الصحيح. **PASS**
  9. تركيب 3 نسخ (كبيرة/صغيرة/قائمة الانتظار) معًا، تركيز متتابع على صف في كل واحدة → مالك واحد فقط في كل لحظة (`windowListeners.size===1`)، والـPaste يصل حصريًا لآخر نسخة تم التركيز عليها، بلا أي تسريب لبقية النسخ. **PASS**
  - **جميع الحالات التسع: PASS** (`node sim_test.js` → `ALL SIMULATED SCENARIOS PASSED`).
  - **تنبيه صريح ومهم:** هذه محاكاة برمجية للخوارزمية (Model-level) تتحقق من صحة المنطق كما هو مكتوب، **وليست اختبارًا حيًا في متصفح حقيقي بـReact/DOM فعليَين** (لا سيرفر ولا DB ولا متصفح متاحين في هذه البيئة). سلوك React الفعلي لتفويض حدث `focus`/`focusin` عبر الـBubbling وتوقّفه بـ`stopPropagation` هو سلوك موثَّق ومعروف في React، لكنه **لم يُشاهَد حيًّا في هذه الجلسة** — لذلك لا تُعتبَر هذه الحالات "مُختبَرة بمتصفح حقيقي"، بل "مُتحقَّق منها بمراجعة الكود + محاكاة منطقية للخوارزمية + Build ناجح".
- **Syntax/Build:** أُعيد استخراج كتلة `<script type="text/babel">` (بدون أي تعديل عليها) وتحويلها عبر `esbuild` محليًا — نجحت بدون أي خطأ (نفس نتيجة جلسة الإصلاح السابقة، مُعاد التحقق منها الآن).
- **ما لم يُختبَر ولا يجوز اعتباره Tested:** أي تشغيل حي في متصفح فعلي (Chrome/Firefox حقيقي) مع Backend/DB يعملان، بما يشمل تجربة Ctrl+V الفعلية بالماوس ولوحة المفاتيح، ولصق صور حقيقية من الحافظة (Clipboard) الفعلية، وملاحظة الـUI (اللون البرتقالي على الصف المستهدَف، الـSpinner، الـToast) بصريًا. لم يتوفر سيرفر ولا متصفح حقيقي في هذه الجلسة.
- **خلاصة:** الإصلاح الحالي (من الجلسة السابقة) **سليم منطقيًا ومطابق للقاعدة المطلوبة** بناءً على كل الأدلة المتاحة في هذه البيئة (مراجعة كود + محاكاة + Build). لم تُكتشَف أي ثغرة أو حالة فشل. **التوصية الوحيدة المتبقية: اختبار حي فعلي في متصفح** لإغلاق الفجوة الوحيدة المتبقية (Live Browser Test)، وهذا غير ممكن في هذه البيئة الحالية.

### ✅ Notifications — تصنيف + فلترة + صفحة كاملة — COMPLETED 🆕 (هذه الجلسة)
**القرارات التصميمية (طُرحت على المستخدم صراحةً قبل الكتابة، بحسب ما طلبه HANDOFF من الجلسة السابقة):**
- **التصنيف (Type):** حسب المصدر فقط — `machine` / `file` / `workday` (اختيار المستخدم صراحةً، وليس حسب الأهمية).
- **الفلترة المطلوبة:** فلترة حسب النوع + صفحة كاملة لعرض كل الإشعارات (بدل آخر 6 فقط في القائمة المنسدلة) — كلاهما اختيار المستخدم صراحةً.
- **أحداث جديدة تُولّد إشعارات:** المستخدم ترك القرار للـAI بناءً على منطق المشروع. **القرار المُتَّخذ:** لم تُضَف أي فئة أحداث جديدة تمامًا؛ الإصلاح الوحيد كان تصحيح تناسق ناقص كان موجودًا مسبقًا في الكود (`bulk-move` كان يُصدر إشعارًا، بينما `bulk-delete` المطابق له في الأهمية لم يكن يُصدر أي إشعار إطلاقًا) — تمت إضافة إشعار لـ`bulk-delete` فقط لمطابقة النمط الموجود بالفعل، دون اختراع أي فئة حدث جديدة غير مطلوبة (مثل: تعديل اسم/وقت الملف، رفع صورة معاينة، تعليق نصي/صوتي، إغلاق يوم عمل) لتفادي توسيع النطاق (Scope Creep) بلا طلب صريح.

**Backend:**
- `database.sql`: أُضيف عمود `type ENUM('machine','file','workday') NOT NULL DEFAULT 'file'` + `KEY idx_type` إلى جدول `notifications`. بيانات الـSeed حُدِّثت لتشمل القيمة الافتراضية.
- `migration_notifications.sql` 🆕: ملف ترقية بنفس نمط ملفات الهجرة الأخرى (`ALTER TABLE ... ADD COLUMN` + `ADD KEY`)، مع ملاحظة صريحة أن الإشعارات القديمة قبل الترقية ستُصنَّف تلقائيًا كـ`'file'` (القيمة الافتراضية) لعدم وجود طريقة موثوقة لاستنتاج مصدرها الأصلي من نص الرسالة فقط.
- `backend/src/utils/events.js`: `addNotification(io, message, type='file')` أصبحت تقبل معامل `type` ثالث اختياري (افتراضي `'file'` للتوافق العكسي)، وتُدرِجه في `INSERT`.
- `backend/src/routes/machines.js`: كل نداءات `addNotification` الخمسة الموجودة مسبقًا الآن تمرِّر `type` صريحًا (`'machine'` لتشغيل/إيقاف الماكينة، `'file'` لبقية العمليات: إضافة ملف بالاسم، رفع ملف، نقل فردي، نقل جماعي). 🆕 أُضيف نداء `addNotification` سادس (`type:'file'`) داخل `DELETE /files/bulk-delete` (لم يكن موجودًا إطلاقًا سابقًا) — انظر تبرير القرار أعلاه.
- `backend/src/routes/workdays.js`: نداء `addNotification` الوحيد الموجود (بدء يوم عمل) الآن يمرِّر `type:'workday'` صراحةً. لم يُضَف أي نداء جديد (مثلاً عند إغلاق اليوم) — بنفس منطق عدم توسيع النطاق أعلاه.
- `backend/src/routes/notifications.js` (أُعيد كتابته بالكامل، لا يزال بنفس الحجم تقريبًا):
  - `GET /` — **لم يتغيّر شكل استجابتها إطلاقًا** (مصفوفة مباشرة، آخر 30، بلا فلترة) — تبقى مخصَّصة حصريًا لجرس الإشعارات (القائمة المنسدلة) في أعلى الواجهة، لضمان عدم كسر أي كود قديم يعتمد على شكل الاستجابة هذا.
  - `GET /all` 🆕 — Endpoint منفصل تمامًا مخصَّص للصفحة الكاملة الجديدة. يدعم `?type=machine|file|workday` (تجاهل صامت لأي قيمة غير صالحة) و`?page=` و`?limit=` (حد أقصى 100 لكل صفحة، افتراضي 20). يُرجع `{items, total, page, limit}` (شكل استجابة مختلف عمداً عن `GET /` لتفادي أي التباس).
  - `PATCH /read-all` — لم يتغيّر.
  - **لا تعارض ترتيب مسارات:** لا يوجد أي مسار عام بصيغة `/:id` في هذا الملف حاليًا، فلا حاجة لحذر الترتيب الذي ينطبق على `bulk-move`/`bulk-delete` في `machines.js` — لكن أُضيفت ملاحظة صريحة في الكود نفسه تُحذِّر من ذلك لو أُضيف مسار عام مستقبلاً.

**Frontend (`frontend/index.html`):**
- ثابتان جديدان على مستوى الملف: `NOTIF_TYPES` (مصفوفة `['machine','file','workday']`) و`NOTIF_TYPE_CONFIG` (تسمية عربية + أيقونة + لون لكل نوع: ماكينات=برتقالي/`laser`، ملفات=أزرق/`clip`، أيام عمل=أخضر/`cal`).
- `Api.getAllNotifications({type,page,limit})` 🆕 دالة جديدة تبني query string وتستدعي `GET /api/notifications/all`.
- جرس الإشعارات (القائمة المنسدلة) في الـTopbar: كل إشعار الآن يعرض أيقونة/لون نوعه (نفس `NOTIF_TYPE_CONFIG`)، وأُضيف زر "عرض كل الإشعارات" في أسفل القائمة يغلقها وينقل لصفحة `notifications` الجديدة (`sp("notifications")`).
- `NotificationsPage` 🆕 (component جديد كامل، بين `LogsPage` و`WorkDaysPage`): تبويبات فلترة حسب النوع (الكل + 3 أنواع، بأيقونة/لون كل نوع)، قائمة الإشعارات (بنفس تنسيق صف `LogsPage` تقريبًا: أيقونة النوع + الرسالة + الوقت، مع تمييز غير المقروء بخلفية مختلفة)، ترقيم صفحات (زرَّا "السابق"/"التالي" + "صفحة X من Y")، وزر "تحديد الكل كمقروء" في الأعلى (يستدعي `onMarkAllRead` من `App` ثم يُعيد تحميل الصفحة الحالية).
- عنصر تنقل جديد `{id:"notifications", l:"الإشعارات", n:"bell"}` أُضيف لكلٍّ من `navAdmin` **و**قائمة تنقل المشغّل (Operator) — الصفحة متاحة لكل الأدوار (نفس منطق ظهور جرس الإشعارات نفسه لكل الأدوار حاليًا، لم يُقيَّد بصلاحية Admin فقط).
- ربط العرض: `{page==="notifications"&&<NotificationsPage addToast={addToast} onMarkAllRead={markRead}/>}` أُضيف في شجرة الـRender الرئيسية في `App` (بدون أي قيد صلاحية `role`).
- **لم يُلمَس `pasteTargetId` ولا `selectedFileIds` إطلاقًا** (تحقَّق: لا علاقة لهذه الميزة بـ`AdminTable` إطلاقًا — كل التعديلات في `App` وملفات جديدة/معزولة).

**نتيجة الاختبارات (هذه الجلسة):**
- **Babel/JSX Compile فعلي حقيقي (تحسين منهجي عن الجلسات الأقدم):** تم العثور على `esbuild` مثبَّتاً محلياً (ضمن حزمة `tsx` الموجودة مسبقاً في البيئة) واستُخدِم لتحويل كتلة `<script type="text/babel">` كاملةً (بعد كل التعديلات) فعليًا من JSX إلى JS — **نجح بدون أي خطأ**، ثم `node --check` على ناتج التحويل — **نجح أيضًا**. هذه أول جلسة منذ عدة جلسات يتم فيها تشغيل تحويل JSX حقيقي فعليًا (وليس فقط فحص توازن الأقواس)، بفضل توفر `esbuild` هذه المرة تحديداً.
- `node --check` على كل ملفات الـBackend المعدَّلة (`events.js`, `machines.js`, `workdays.js`, `notifications.js`, `server.js`) — نجح جميعها بدون استثناء.
- بحث نصي: كل نداءات `addNotification` الستة (5 قديمة + 1 جديد) تمرِّر `type` صريحًا الآن — تم التحقق يدويًا سطرًا بسطر.
- **لم يُختبَر:** أي تشغيل حي (MariaDB + Node + متصفح حقيقي) — لا بيئة تشغيل حي متاحة في هذه الجلسة كسابقاتها. يجب تشغيل `migration_notifications.sql` (أو استخدام `database.sql` الجديد لقاعدة بيانات جديدة) قبل أي اختبار حي، وإلا سيفشل أي `INSERT INTO notifications` بعمود `type` غير موجود.

### ✅ Statistics — بُنيَت بالكامل 🆕 (هذه الجلسة) — Backend + Frontend، غير مختبَرة حيًا بعد
**القرارات التصميمية (طُرحت على المستخدم صراحةً قبل الكتابة، كما أوصت الجلسة السابقة):**
- **وقت الماكينة:** المستخدم ترك التصميم للـAI صراحةً. **القرار المُتَّخذ:** لم يُمَس `machine_time` الحالي إطلاقًا (تحقَّقتُ أنه فعليًا **غير معروض في أي مكان بالواجهة حاليًا** — بحث نصي `machine_time`/`machineTime` في `frontend/index.html` = صفر نتائج، حقل خامل تمامًا). بدلاً من ذلك أُضيفت طبقة **Runtime فعلي جديدة كلياً ومنفصلة تمامًا**: عمود `machines.running_started_at` (يُسجَّل عند RUNNING، يُصفَّر عند STOPPED) + جدول جديد `machine_runtime_logs` (صف واحد لكل دورة RUNNING→STOPPED مكتملة، بمدة محسوبة ومخزَّنة فعليًا `duration_seconds`). هذا تصميم غير هدّام تمامًا (Additive) — لا خطر على أي كود موجود.
- **Operator Sessions:** المستخدم اختار صراحةً "مدة جلسة الحضور/الانصراف (Login → Logout)". جدول جديد `operator_sessions` يُفتَح صف فيه عند `POST /api/auth/login` ويُغلَق عند `POST /api/auth/logout` (`logout_at` + `duration_seconds` محسوبة عبر `TIMESTAMPDIFF`).
  - **قيد معروف موثَّق صراحةً (وليس Bug):** أي جلسة لم تُغلَق عبر `/logout` صراحةً (مثال: المستخدم يغلق التبويب مباشرة بدل الضغط على "تسجيل خروج") تبقى `logout_at = NULL` **إلى الأبد** — لا يوجد أي منطق يُغلقها تلقائيًا عند تسجيل دخول تالٍ (تعمُّدًا، تفاديًا لاختلاق وقت خروج غير حقيقي). هذه الجلسات المفتوحة **تُستبعَد من إجمالي المدة** في تقرير Statistics، وتُعرَض بصريًا كعدّاد منفصل ("+N جلسة مفتوحة") حتى يعرف المشرف أن هناك بيانات غير مكتملة لتلك الفترة/المشغّل، بدل تصفير صامت أو حساب خاطئ.
- **قاعدة منتصف الليل — 🆕 أُعيد تصميمها وتنفيذها فعليًا في جلسة لاحقة (هذه الجلسة)، وهذا القسم كان موثِّقًا للنسخة القديمة المُستبدَلة:**
  - **النسخة القديمة (كانت موصوفة هنا سابقًا، أُلغيت):** "تُحسب بالكامل على يوم البداية" — أي دورة تشغيل/جلسة تعبر منتصف الليل كانت تُنسَب **بالكامل** ليوم بدايتها فقط (`DATE_FORMAT(started_at,'%Y-%m-%d')`/`DATE_FORMAT(login_at,'%Y-%m-%d')`)، فلو مثلاً اشتغلت ماكينة من 11:00 مساءً حتى 2:00 فجرًا، كانت الساعتان بعد منتصف الليل تُحسَبان (بالخطأ) ضمن اليوم الأول، واليوم الثاني يظهر بصفر رغم أنه شهد ساعتين تشغيل فعليتين. نفس المشكلة كانت تنطبق أيضًا على حدود الأسبوع/الشهر نفسها (دورة تبدأ قبل بداية المدى المطلوب وتنتهي داخله كانت تُستبعَد بالكامل من التقرير، ودورة تبدأ داخل المدى وتنتهي بعده كانت تُحسَب بالكامل رغم أن جزءًا منها خارج الفترة المطلوبة).
  - **النسخة الجديدة (المُطبَّقة الآن فعليًا):** كل دورة تشغيل (`machine_runtime_logs`) أو جلسة مشغّل مغلقة (`operator_sessions` بـ `logout_at IS NOT NULL`) تُقسَّم على مستوى **الثانية** بين كل الأيام التقويمية التي تمر بها فعليًا `started_at→stopped_at`/`login_at→logout_at`، مع قصّ الفترة أيضًا على حدود المدى المطلوب (أسبوع/شهر) نفسه — لا فقدان ولا ازدواج لأي ثانية. مثال الماكينة أعلاه: الساعة 11–12 تُحسَب لليوم الأول، والساعتان 12–2 تُحسَبان لليوم الثاني، بدقة كاملة.
  - **مكان التنفيذ:** دالة جديدة `splitIntervalByDay(startStr, endStr, rangeStartStr, rangeEndStr)` في `backend/src/utils/time.js`، تعمل على نصوص DATETIME خام (`{ dateStrings: true }` في استعلامات SQL المعنية تحديدًا) بدون أي تحويل عبر كائن `Date` الخاص بالـdriver (تفاديًا تمامًا لأي انزياح منطقة زمنية — بنفس الحذر الموثَّق سابقًا في هذا القسم بخصوص أعمدة DATE). تستخدمها `backend/src/routes/statistics.js` في كلا الـendpoint (`/machines` و`/operators`) بعد جلب الصفوف المتقاطعة زمنيًا مع المدى المطلوب (`started_at < range.end AND stopped_at > range.start`)، بدل التجميع المباشر بـ`GROUP BY DATE_FORMAT(...)` في SQL.
  - **`resolveRange` تغيّر أيضًا:** يُرجِع الآن مدى **half-open** `[start, end)` دائمًا (`end` = اليوم التالي لآخر يوم في المدى الساعة `00:00:00`، بدل `23:59:59` لآخر يوم) — هذا يجعل مقارنات التداخل/القصّ في `splitIntervalByDay` بسيطة وصحيحة رياضيًا بلا حالات حافة.
  - **اختبار الوحدة (Unit Test):** كُتِب سكربت اختبار يدوي مؤقت (غير محفوظ كملف دائم في المشروع) غطّى 8 حالات: دورة ضمن يوم واحد، دورة تعبر منتصف الليل (23:00→02:00، تحقَّق التقسيم 1س/2س)، دورة تمتد 3 أيام كاملة، دورة تبدأ قبل بداية المدى (قصّ صحيح)، دورة تنتهي بعد نهاية المدى (قصّ صحيح)، إضافة يوم عبر نهاية شهر وعبر نهاية سنة، ودورة خارج المدى بالكامل (نتيجة فارغة) — **كل الحالات الثماني نجحت (PASS)**. هذا اختبار منطق حسابي مباشر (Node، بدون DB حقيقية)، **وليس اختبارًا حيًا للـAPI كاملاً** (لا يزال يحتاج تشغيل سيرفر + MariaDB حقيقيين وبيانات فعلية تعبر منتصف الليل، لم يحدث بعد).
  - `database.sql` (تعليق `machine_runtime_logs`) و`migration_statistics.sql` **لم يتغيّر أي منهما بنيويًا** — لا حاجة لأي migration جديد، لأن `started_at`/`stopped_at`/`login_at`/`logout_at` كانت مخزَّنة بدقة كاملة أصلًا؛ التقسيم يحدث فقط عند **القراءة** (وقت بناء التقرير)، وليس عند الكتابة. تم فقط تحديث نص التعليق التوضيحي في `database.sql` ليعكس السلوك الجديد بدقة.
- **قرار تصميمي إضافي اتُّخذ ولم يُطرَح صراحةً (يستحق مراجعة المستخدم):** لم أعتمد على `work_days`/`work_day_id` (نظام "أيام العمل" المُدار يدويًا من المشرف) في تجميع Statistics إطلاقًا — بدلاً من ذلك استُخدِم التاريخ الفعلي (`DATE(started_at)`) والنطاق الزمني الفعلي لجدول `work_weeks` (`start_date`/`end_date`) مباشرة كفلتر SQL. **السبب:** هذا يجعل الإحصائيات صحيحة حتى لو نسي المشرف فتح "يوم عمل" رسمي في نظام Work Days، ولا يعتمد على انضباط استخدام تلك الميزة. **الأثر العملي:** أسبوع Statistics = المدى الزمني الفعلي لتواريخ `work_weeks.start_date`→`end_date`، وليس بالضرورة نفس مجموعة الأيام المُفعَّلة فعليًا في `work_days` لذلك الأسبوع (قد يختلفا إن كانت أيام العمل غير منضبطة). **يحتاج تأكيد المستخدم أن هذا هو المنطق المطلوب.**

**Backend (تفاصيل الملفات):**
- `database.sql`: عمود `machines.running_started_at DATETIME DEFAULT NULL` جديد + تعليق مُحدَّث على `machine_time` يوضح صراحةً أنه ليس Runtime فعلي. جدولان جديدان كاملان: `machine_runtime_logs` (FK إلى `machines` CASCADE و`users` SET NULL) و`operator_sessions` (FK إلى `users` CASCADE — القرار: حذف مستخدم يحذف سجل جلساته أيضاً، متسق مع أن استعلام `/operators` يبدأ أصلاً من `FROM users` فلا قيمة لإبقاء صفوف يتيمة).
- `migration_statistics.sql` 🆕: ملف ترقية بنفس نمط الملفات الأخرى، مع ملاحظة صريحة أن أي تشغيل/جلسة **قبل** تشغيل هذه الترقية لن تظهر في أي تقرير مستقبلاً (لا بيانات تاريخية عنها) — هذا متوقَّع وليس خطأ.
- `backend/src/routes/machines.js`: `PATCH /:code/toggle` الآن يفرّق بين الانتقال لـRUNNING (يسجّل `running_started_at=NOW()` فقط) والانتقال لـSTOPPED (يحسب `TIMESTAMPDIFF(SECOND,...)` من `running_started_at` المخزَّن قبل هذا الطلب، يُدرِج صفاً في `machine_runtime_logs`، ثم يصفّر `running_started_at`). **حالة حافة مُعالَجة صراحةً بتعليق بالكود:** إن كانت `running_started_at` فارغة رغم أن الحالة كانت RUNNING (بيانات قديمة من قبل هذه الترقية)، يُتجاهَل تسجيل Runtime لهذه الدورة فقط دون أي تأثير على تبديل الحالة نفسه أو أي خطأ يظهر للمستخدم.
- `backend/src/routes/auth.js`: `POST /login` يُدرِج صف `operator_sessions` جديد بعد نجاح الدخول مباشرة (لكل الأدوار، وليس `role='operator'` فقط — انظر لماذا في قسم Frontend أدناه). `POST /logout` يبحث عن آخر جلسة مفتوحة (`logout_at IS NULL`) لنفس المستخدم فقط (`ORDER BY id DESC LIMIT 1`) ويغلقها.
- `backend/src/routes/statistics.js` 🆕 (ملف جديد كامل، مُركَّب على `/api/statistics`، `requireAdmin` على كل مساراته — نفس نمط `users`/`logs`/`workdays`):
  - `GET /machines?scope=week&weekId=` أو `?scope=month&year=&month=` → إجمالي/عدد تشغيلات لكل من الماكينة الكبيرة/الصغيرة + توزيع يومي (للرسم البياني في الواجهة). `queue` مستبعدة دائماً (لا يمكن تشغيلها أصلاً، Route الأصلي يمنع ذلك).
  - `GET /operators?scope=week|month&...` → لكل مستخدم `role='operator'` (فقط، الأدمن مستبعد من هذا التقرير تحديداً): إجمالي وقت الجلسات **المُغلَقة فقط** + عدد الجلسات المغلقة + عدد الجلسات المفتوحة (منفصل، ليس مطروحاً بصمت).
  - **Bug تاريخي (من جلسة سابقة) كان موثَّقًا هنا:** إدراج قيمة عمود DATE من MySQL مباشرة داخل template literal نصي كان سينتج نصاً غير صالح كمعامل SQL — أُصلِح وقتها باستخدام `DATE_FORMAT(start_date,'%Y-%m-%d')` صراحةً في `resolveRange`. **هذا الجزء لا يزال صحيحًا ولم يتغيّر.**
  - 🆕 **التوزيع اليومي تغيّر بالكامل هذه الجلسة:** لم يعد يعتمد على `GROUP BY DATE_FORMAT(started_at,...)` في SQL (كان هذا هو أساس مشكلة "قاعدة منتصف الليل" القديمة — كل صف كامل يُنسَب ليوم *بداية* السطر فقط بحكم بنية `GROUP BY`). الآن: تُجلَب الصفوف الخام المتقاطعة مع المدى (`{ dateStrings: true }`, `WHERE started_at < range.end AND stopped_at > range.start`)، ثم تُقسَّم كل واحدة عبر `splitIntervalByDay` في الكود (JS) — انظر تفاصيل كاملة في قسم "قاعدة منتصف الليل" أعلاه. نفس المبدأ طُبِّق حرفيًا على `/operators` (`operator_sessions` بدل `machine_runtime_logs`).
  - `resolveRange` أصبحت تُرجِع `{ start, end, label }` بدل `{ start, end }` بنفس الأسماء لكن دلالة `end` تغيّرت من "نهاية اليوم الأخير `23:59:59`" إلى "بداية اليوم التالي لآخر يوم `00:00:00`" (half-open) — كل استعلامات الـSQL المتبقية (`YEAR()/MONTH()` القديمة لنطاق الشهر) استُبدِلت أيضًا بمقارنة `start`/`end` صريحة، لتوحيد المنطق بين `week` و`month` ولإتاحة نفس آلية القصّ لكليهما.
- `backend/src/server.js`: سطر تركيب واحد جديد: `app.use('/api/statistics', require('./routes/statistics')());`.

**Frontend (`frontend/index.html`):**
- `Api.getMachineStats({scope,weekId,year,month})` و`Api.getOperatorStats({...})` 🆕 — بناء query string وطلب GET، نفس نمط `getAllNotifications`.
- أيقونة SVG جديدة `chart` (أعمدة بسيطة) أُضيفت لخريطة الأيقونات، لعدم وجود أيقونة إحصائيات مناسبة سابقاً.
- `StatisticsPage` 🆕 (component جديد كامل): تبديل نطاق (أسبوعي/شهري)، قائمة أسابيع (تُجلَب عبر `Api.getWorkWeeks()` الموجودة مسبقاً — لا endpoint مكرر)، منتقي شهر/سنة للنطاق الشهري، بطاقات إجمالي لكل ماكينة + عدد تشغيلات، رسم بياني يدوي بسيط (أشرطة CSS/div، **بدون أي مكتبة رسم بيانية خارجية** — المشروع لا يحمّل أي مكتبة كهذه عبر CDN حالياً (React/ReactDOM/Babel-standalone/Socket.io فقط)، وإضافة واحدة جديدة تُعتبر تغييراً معماريًا لم يُطلَب، فتم تفاديه عمداً) لتوزيع الوقت اليومي داخل الفترة المختارة، وجدول جلسات المشغّلين (اسم + إجمالي وقت + عدد جلسات مغلقة + شارة "جلسة مفتوحة" منفصلة إن وُجدت).
- عنصر تنقل جديد `{id:"statistics", l:"الإحصائيات", n:"chart"}` أُضيف **لـ`navAdmin` فقط** (وليس لقائمة تنقل Operator) — قرار: هذه صفحة تحليلية للمشرف فقط، بنفس نمط `users`/`logs`/`workdays` الحالية، **لم يُطرَح صراحةً على المستخدم ويستحق تأكيداً**. الحماية مزدوجة: غياب عن نفس التنقل لغير الأدمن + شرط `user?.role==="admin"` صريح عند الـRender، بنفس نمط بقية صفحات الأدمن.
- (ملاحظة تاريخية: عند إنشاء `StatisticsPage`، كانت `SettingsPage` لا تزال موجودة كما هي — حذفها تم لاحقًا في جلسة منفصلة، انظر قسم Settings أدناه.)
- **بحث تصحيح ثانٍ اكتُشِف وأُصلِح أثناء المراجعة الذاتية:** قائمة الأسابيع المنسدلة في `StatisticsPage` كانت ستعرض `start_date`/`end_date` كنص ISO كامل غير منسَّق (لأن استجابة `GET /api/work/weeks` تمرّ عبر `JSON.stringify` القياسي الذي يحوّل JS `Date` تلقائياً لصيغة ISO datetime كاملة، وليس تاريخاً مبسَّطاً) — **أُصلِح** باستخدام دالة `fmtDateStr()` **الموجودة مسبقاً في المشروع** (تُستخدَم فعلاً في `WorkDaysPage` لنفس الغرض بالضبط)، بدل ابتكار تنسيق جديد أو عرض النص الخام.
- **لم يُلمَس `pasteTargetId` ولا `selectedFileIds` ولا نظام Notifications إطلاقًا** — كل التعديلات في ملفات/أقسام جديدة ومعزولة تماماً.

**نتيجة الاختبارات (هذه الجلسة، لهذه الميزة تحديدًا):**
- **Babel/JSX Compile فعلي حقيقي (`esbuild`) — أُعيد تشغيله مرتين** (قبل وبعد إصلاح الـBugين أعلاه) — **نجح في المرتين بدون أي خطأ**، ثم `node --check` على الناتج المُحوَّل في كل مرة — نجح أيضاً.
- `node --check` على كل ملفات الـBackend الجديدة/المعدَّلة (`statistics.js` الجديد، `machines.js`, `auth.js`, `server.js`) — نجح جميعها.
- فحص توازن الأقواس/الأقواس المعقوفة/الحاصرة لكامل سكربت الـFrontend بعد كل التعديلات — متوازن تماماً.
- **لم يُختبَر إطلاقًا (حينها):** أي تشغيل حي (لا MariaDB ولا Node ولا متصفح متاحين في هذه الجلسة). يجب تشغيل `migration_statistics.sql` (بعد `migration_notifications.sql`) على أي قاعدة بيانات موجودة مسبقاً قبل أي اختبار حي.

**🆕 نتيجة الاختبارات (جلسة إكمال قاعدة منتصف الليل الحقيقية):**
- `node --check` على `backend/src/utils/time.js` (المعدَّل) و`backend/src/routes/statistics.js` (أُعيد كتابته بالكامل) — نجح كلاهما.
- **اختبار وحدة (Unit Test) مباشر بـ Node لدالة `splitIntervalByDay` و`addDaysToDateStr`** — 8 حالات: دورة ضمن يوم واحد (PASS)، دورة تعبر منتصف الليل 23:00→02:00 وتتوزّع 1س/2س بين يومين (PASS)، دورة تمتد 3 أيام متتالية كاملة (PASS)، دورة تبدأ قبل بداية المدى فتُقصّ عليه (PASS)، دورة تنتهي بعد نهاية المدى فتُقصّ عليه (PASS)، إضافة يوم عبر نهاية شهر 30 يوم (PASS)، إضافة يوم عبر نهاية سنة ميلادية (PASS)، دورة خارج المدى بالكامل فتُعيد مصفوفة فارغة (PASS) — **كل الحالات الثماني نجحت**. هذا السكربت كان مؤقتًا لغرض التحقق فقط ولم يُحفَظ كملف دائم في المشروع.
- **لم يتغيّر:** لا `database.sql` بنيويًا (عمود/جدول) ولا `migration_statistics.sql` — فقط نص تعليق توضيحي في `database.sql` تم تحديثه ليطابق السلوك الجديد. لا حاجة لأي migration إضافي لمن شغّل `migration_statistics.sql` سابقًا.
- **لم يُختبَر إطلاقًا (لا يزال):** أي تشغيل حي كامل (سيرفر + MariaDB حقيقيين + بيانات runtime/session تعبر منتصف الليل فعليًا + فحص بصري للأرقام في `StatisticsPage`). **المستخدم أكّد صراحةً في هذه الجلسة أنه لم يشغّل أي اختبار حي لأي ميزة متراكمة بعد** (ليس فقط Statistics — كل الميزات الخمس السابقة أيضًا: Work Days، Multi-Select، Bulk Move/Delete، Notifications، Statistics). هذا أهم بند معلّق في المشروع حاليًا.

### ✅ Settings — محذوفة نهائيًا 🆕 (بند 7 مكتمل، استُبدِلت بـStatistics)
- `SettingsPage` **كانت** موجودة وتظهر في التنقل (`{id:"settings", l:"الإعدادات", n:"cog"}`)، ومحتواها كان ديكوريًا بالكامل: 4 بطاقات ثابتة (سرعة القص، طاقة الليزر... إلخ) بمفاتيح تبديل (toggle switches) **غير مربوطة بأي state أو API**.
- **حُذِفت بالكامل هذه الجلسة**: دالة `SettingsPage`، عنصرها في `navAdmin`، وسطر الـrender `page==="settings"&&<SettingsPage/>`. لا وجود لأي مرجع متبقٍ لها في `frontend/index.html` (تم التحقق بالبحث الشامل).
- **القرار الذي سمح بالتنفيذ:** توصية "DO NOT CHANGE" السابقة كانت تشترط اختبارًا حيًا لـStatistics أولاً قبل الحذف — لم يتحقق هذا الشرط، لكن **المستخدم طلب صراحةً المتابعة رغم ذلك** بعد أن سُئل مباشرة وأجاب بأنه لم يشغّل أي اختبار حي. هذا قرار موثَّق من المستخدم، وليس تجاوزًا من الـAI لتوصية "DO NOT CHANGE" (انظر التنبيه أعلى الملف و`LAST WORKING STATE`).
- **الأثر العملي:** لا توجد الآن أي صفحة "إعدادات" في النظام إطلاقًا — من يبحث عن `SettingsPage` أو `id:"settings"` لن يجدها. Statistics هي الصفحة الوحيدة في مكان التنقل الذي كانت تشغله Settings سابقًا.

### ✅ Socket.io / Auto-Sync — البند 8 مكتمل 🆕 (توضيح المستخدم: لا حاجة لـF5 أبدًا لرؤية التحديثات + Auto-Sync احتياطي عند انقطاع الاتصال)
- `server.js` يستخدم مصادقة JWT على اتصال Socket.io، يتتبع Online/Offline عبر `presence.js`، ويضع المشرفين في غرفة `admins`. (لم يتغيّر.)
- **الفحص الذي تم قبل أي تعديل:** بحث شامل عن كل `io.emit`/`io.to(...).emit` في الباك إند كله. النتيجة: كل الـ19 endpoint في `machines.js` (رفع/حذف/نقل/تعليق/صوت/thumbnail/bulk-move/bulk-delete/reorder... إلخ) **كانت بالفعل** تبث `machine:update` بعد كل تعديل — **لا نقص هناك**. `workdays.js` يبث `workday:update`/`workweek:new` (موجود مسبقًا). `events.js` يبث `log:new`/`notif:new` لكل الأحداث (موجود مسبقًا).
- **الفجوة الحقيقية الوحيدة المكتشَفة:** `backend/src/routes/users.js` (إضافة مستخدم `POST /` وحذف مستخدم `DELETE /:id`) **لم يكن يبث أي حدث Socket.io إطلاقًا** — كان `io` مُمرَّراً للملف لكن غير مُستخدَم فيه. أثر ذلك: مشرف يضيف/يحذف مستخدماً، ومشرف آخر متصل بنفس اللحظة لن يرى التغيير في `UsersPage` إلا بعد F5 أو حتى يعمل هو نفسه أي إجراء يستدعي `refreshUsers()`. **هذا كان الفجوة الوحيدة الفعلية في "لا حاجة لـF5" عبر كل المشروع.**
- **الإصلاح (`backend/src/routes/users.js`):** بعد إنشاء المستخدم بنجاح، `io.to('admins').emit('user:new', newUser)`. بعد حذفه بنجاح، `io.to('admins').emit('user:deleted', {id})`. البث لغرفة `admins` فقط (بنفس نمط بث `queue` في `machines.js`) لأن صفحة المستخدمين للمشرفين حصراً. `node --check` نجح على الملف بعد التعديل.
- **الإصلاح (`frontend/index.html`, داخل `App`):** أُضيف مستمعان جديدان `socket.on("user:new",...)` و`socket.on("user:deleted",...)` يحدّثان `users` state مباشرة (بدون أي refetch) — بنفس نمط `workday:update` الموجود مسبقاً.
- **🆕 Auto-Sync الاحتياطي (الجزء الذي طلبه المستخدم صراحةً لتغطية انقطاع الاتصال):**
  - `socket.on("disconnect", ...)`: يبدأ `setInterval` كل 20 ثانية يستدعي `refreshAll()` (+`refreshUsers()`/`refreshWorkDays()` إن كان المستخدم مشرفاً) — **هذا الـinterval يعمل فقط أثناء انقطاع الاتصال**.
  - `socket.on("connect", ...)`: يوقف أي `interval` فوري، وإن كان هذا اتصالاً *لاحقاً* (إعادة اتصال بعد انقطاع، وليس أول اتصال عند فتح الصفحة) يستدعي جلباً فورياً واحداً لالتقاط أي تحديثات فاتت أثناء الانقطاع (متغيّر محلي `hasConnectedBefore` يمنع تكرار الجلب عند أول تحميل للصفحة، لأن البيانات مجلوبة أصلاً هناك عبر `useEffect` منفصل بعد تسجيل الدخول).
  - **هذا يخالف جزئيًا** بند DO NOT CHANGE القديم "لا تُضِف أي polling" — التخالف **مقصود وبطلب صريح من المستخدم** (انظر توضيحه الحرفي في بداية هذه الجلسة)، وتم تحديث ذلك البند بوضوح ليعكس الاستثناء (انظر قسم DO NOT CHANGE).
  - **لم تُمَس Statistics إطلاقًا** — لا تزال بلا Socket.io ولا أي إشعار بالتحديث، بنفس النمط المتعمَّد سابقًا (جلب عند فتح الصفحة/تغيير الفلتر فقط). المستخدم لم يطلب تغيير هذا تحديدًا، والـfallback الجديد لا يستدعي أي دالة جلب خاصة بـStatistics.
- **نتيجة الاختبارات:** `node --check` على `users.js` نجح. تحقّق `@babel/core` (`transformSync`, preset-react+preset-env) على كامل `frontend/index.html` بعد كل التعديلات نجح بدون أخطاء نحوية. **لم يُختبَر حيًا/بصريًا بعد** (لا سيرفر ولا متصفح حقيقي متاحان — نفس القيد المتكرر في كل الجلسات السابقة). تحديدًا لم يُختبَر: هل يبدأ الـfallback interval فعليًا عند فصل الشبكة يدويًا في متصفح حقيقي، وهل يتوقف فور عودة الاتصال، وهل حدثا `user:new`/`user:deleted` يصلان فعليًا لمشرف آخر متصل بنفس اللحظة.

---

## FILES ACTUAL STATE (تم التحقق سطرًا بسطر)

| الملف | الحالة |
|---|---|
| `frontend/index.html` (2378 سطر) | Branding + Ctrl+V (ملف واحد فقط — COMPLETED نهائيًا) + Notifications متطور (تصنيف/فلترة/صفحة كاملة) + واجهة Admin كاملة لأيام/أسابيع العمل + Multi-Select + Bulk Move/Delete + **`StatisticsPage` كاملة** (تبديل أسبوعي/شهري، بطاقات إجمالي، رسم أعمدة CSS يدوي، جدول جلسات مشغّلين). لم يتغيّر في جلسة "إكمال قاعدة منتصف الليل" الحالية — التعديل كان Backend فقط. |
| `backend/src/routes/workdays.js` (195 سطر) | موجود بالكامل، 7 endpoints، مسجَّل في `server.js`. لم يتغيّر هذه الجلسة. |
| `backend/src/routes/machines.js` (937 سطر، 21 endpoint) | `PATCH /files/bulk-move` و`DELETE /files/bulk-delete` مُعرَّفان قبل `/files/:fileId` العام. `PATCH /:code/toggle` يسجّل `running_started_at` عند RUNNING ويُدرِج صفًا في `machine_runtime_logs` عند STOPPED. لم يتغيّر هذه الجلسة. |
| `backend/src/routes/auth.js` (81 سطر) | `POST /login` يفتح صف `operator_sessions`، `POST /logout` يغلق آخر جلسة مفتوحة لنفس المستخدم. لم يتغيّر هذه الجلسة. |
| `backend/src/utils/events.js` | `addNotification(io, message, type='file')`. لم يتغيّر هذه الجلسة. |
| `backend/src/routes/notifications.js` (78 سطر) | `GET /` (آخر 30، لجرس الإشعارات) + `GET /all` (فلترة + ترقيم صفحات) + `PATCH /read-all`. لم يتغيّر هذه الجلسة. |
| 🆕 `backend/src/routes/statistics.js` (167 سطر بعد إعادة الكتابة الكاملة هذه الجلسة) | `GET /machines` و`GET /operators`، كلاهما يستخدم الآن `splitIntervalByDay` للتوزيع اليومي/القصّ على حدود المدى بدل `GROUP BY DATE_FORMAT(...)` المباشر. `resolveRange` يُرجِع مدى half-open `[start,end)` موحَّد لـ`week`/`month` معًا. |
| 🆕 `backend/src/utils/time.js` (81 سطر بعد الإضافة هذه الجلسة) | أُضيفت 4 دوال جديدة: `naiveDtToMs`, `msToDateStr`, `addDaysToDateStr`, `splitIntervalByDay` — كلها تتعامل مع نصوص DATETIME خام فقط، بدون أي اعتماد على تحويل Date عبر أي driver. `timeToSeconds`/`secondsToTime` القديمتان لم تتغيّرا. |
| `database.sql` | 9 جداول (`users`, `machines`, `machine_files`, `work_weeks`, `work_days`, `logs`, `machine_runtime_logs`, `operator_sessions`, `notifications`). 🆕 تعديل هذه الجلسة: تحديث نص تعليق `machine_runtime_logs` فقط (ليعكس أن التقسيم يحدث عند القراءة، لا عند الكتابة) — **لا تغيير بنيوي، لا حاجة لأي migration جديد.** |
| `migration_workdays.sql`, `migration_queue.sql`, `migration_comments.sql`, `migration_thumbnail.sql`, `migration_notifications.sql` | كل منها يطابق الجزء المقابل له في `database.sql`. لم تتغيّر هذه الجلسة. |
| `migration_statistics.sql` | يطابق ما هو مدمَج في `database.sql` (عمود `running_started_at` + جدولا `machine_runtime_logs`/`operator_sessions`). لم يتغيّر بنيويًا هذه الجلسة (لا حاجة، انظر أعلاه). |
| `frontend/assets/logo.png`, `logo-mark.png` | موجودان على القرص. |

## TESTING STATUS (مُحدَّث بعد جلسة إكمال قاعدة منتصف الليل)

| البند | الحالة |
|---|---|
| `node --check` على كل ملفات الـBackend (شامل `statistics.js` المُعاد كتابته و`time.js` المُعدَّل) | ✅ نجح |
| 🆕 اختبار وحدة مباشر (Node) لـ`splitIntervalByDay`/`addDaysToDateStr` — 8 حالات (يوم واحد، عبور منتصف الليل، امتداد 3 أيام، قصّ بداية المدى، قصّ نهاية المدى، عبور نهاية شهر، عبور نهاية سنة، خارج المدى بالكامل) | ✅ **كل الحالات الثماني PASS** — سكربت مؤقت، غير محفوظ كملف دائم |
| Babel/JSX Compile فعلي (`esbuild`) لكامل `frontend/index.html` | ⚠️ **لم يُعَد تشغيله هذه الجلسة** — الفرونت لم يتغيّر، وآخر تشغيل موثَّق (جلسة Statistics الأصلية) كان ناجحًا. يُنصَح بإعادته في أول جلسة قادمة تُعدِّل الفرونت مجددًا، وليس بالضرورة قبل ذلك. |
| فحص منطقي: `resolveRange` نصف-مفتوح `[start,end)` موحَّد بين `week`/`month`، الاستعلامات الخام تستخدم `{dateStrings:true}` بدل تحويل Date التلقائي | ✅ تم التحقق يدويًا بقراءة الكود سطرًا بسطر |
| `pasteTargetId`/`selectedFileIds`/Notifications | ✅ لم تُلمَس إطلاقًا هذه الجلسة (تعديلات معزولة في `statistics.js`/`time.js`/`database.sql` فقط) |
| اختبار حي كامل (MariaDB + Node + متصفح حقيقي) لأي ميزة في المشروع (Work Days، Multi-Select، Bulk Move/Delete، Notifications، **Statistics بما فيها قاعدة منتصف الليل**) | ❌ **لم يُنفَّذ إطلاقًا لأي ميزة حتى الآن، بتأكيد صريح من المستخدم هذه الجلسة.** هذا هو أهم بند معلّق في المشروع بأكمله — ست ميزات كبيرة متراكمة بدون أي اختبار بصري واحد بينها. |
| اختبار حي مخصّص لقاعدة منتصف الليل تحديدًا (تشغيل ماكينة فعليًا يعبر منتصف الليل، ثم مقارنة الأرقام في `StatisticsPage` يدويًا) | ❌ لم يُنفَّذ — يحتاج بيئة تشغيل حي + وقت انتظار فعلي (أو تعديل يدوي لبيانات `machine_runtime_logs` في قاعدة بيانات اختبار) |

## DO NOT CHANGE
- لا تُعد تصميم الـLayout العام دون طلب صريح.
- لا تُنشئ نظام Authorization جديد للـBulk Actions أو Notifications أو Statistics — استُخدم `requireAdmin`/`authenticate` الموجودين فقط.
- لا تحذف بيانات الأيام/الأسابيع السابقة عند بدء يوم/أسبوع جديد (القرار: Tag فقط، `ON DELETE SET NULL`).
- **لا تكسر `pasteTargetId`** عند أي تعديل مستقبلي على `selectedFileIds`/Bulk Actions — يجب أن يبقيا منفصلين تمامًا. Ctrl+V **COMPLETED نهائيًا** بقرار ثابت غير قابل لإعادة الفتح: لصق لملف واحد فقط، لا لصق جماعي إطلاقًا.
- **لا تُعِد فتح قرار "أحداث Notifications الجديدة":** لا فئة حدث جديدة عدا ما هو موجود، إلا بتأكيد صريح من المستخدم.
- **لا تُغيِّر شكل استجابة `GET /api/notifications`** (مصفوفة مباشرة، آخر 30، بلا فلترة) — الفلترة/الترقيم حصريًا عبر `GET /api/notifications/all`.
- لا تُعِد ترتيب المسارات في `machines.js` بحيث يسبق `/files/:fileId` (العام) المسارين `/files/bulk-move` و`/files/bulk-delete`.
- ~~لا تحذف `SettingsPage` قبل أن تكون صفحة Statistics البديلة مختبَرة حيًا~~ — **تجاوَزها المستخدم صراحةً**؛ `SettingsPage` محذوفة الآن فعليًا (انظر بند 7 في REMAINING TASKS وقسم Settings أعلاه). لا حاجة أبدًا لإعادة `SettingsPage` أو التراجع عن الحذف دون طلب صريح جديد.
- ~~لا تُضِف أي polling لأيام/أسابيع العمل أو الإشعارات أو Statistics~~ — **مُعدَّل جزئيًا بطلب صريح من المستخدم (بند 8، Socket.io/Auto-Sync)**: أُضيف الآن **fallback polling محدود ومشروط بالاتصال فقط** (كل 20 ثانية، ويعمل حصريًا أثناء انقطاع اتصال Socket.io ويتوقف فور عودته) في `App` بالفرونت — **ليس Polling دائمًا**، ولا يغطي Statistics إطلاقًا (نمطها الحالي — لا تحديث لحظي، جلب عند فتح الصفحة فقط — لم يُمَس). الاعتماد الأساسي لا يزال حصريًا على أحداث Socket.io؛ الـfallback خط دفاع ثانٍ فقط عند انقطاع الاتصال. **لا توسّع هذا الـfallback ليشمل Statistics أو ليعمل بشكل دائم دون طلب صريح جديد.**
- 🆕 **لا تُعِد "قاعدة منتصف الليل" إلى نسخة "تُحسب بالكامل على يوم البداية" المبسَّطة القديمة** دون نقاش صريح مع المستخدم — القرار الحالي (تقسيم حقيقي بالثانية عبر `splitIntervalByDay`) هو المقصود فعليًا، وأي رجوع للخلف هو فقدان دقة مقصود يحتاج تبريرًا صريحًا.
- 🆕 **لا تجلب/تجمّع بيانات `machine_runtime_logs`/`operator_sessions` عبر `GROUP BY DATE_FORMAT(...)` مباشرة في SQL بعد الآن** — هذا بالضبط ما كان يسبب عدم دقة قاعدة منتصف الليل القديمة. أي إحصائية يومية/مجمَّعة جديدة يجب أن تمرّ عبر جلب الصفوف الخام (`{dateStrings:true}`) ثم `splitIntervalByDay` في الكود، بنفس نمط `statistics.js` الحالي.
- 🆕 **لا تفترض أن ماكينة لا تزال RUNNING تساهم بأي ثانية في Statistics قبل أن تتوقف فعليًا** — التصميم الحالي متعمَّد: الـRuntime لا يُسجَّل في `machine_runtime_logs` إلا عند الانتقال لـSTOPPED (`duration_seconds` محسوبة وقتها). ماكينة تعمل حاليًا (حتى لو لساعات) تُحسَب صفرًا في أي تقرير حتى تتوقف. هذا قيد معروف، وليس خطأ — تغييره (لعرض وقت جزئي "حي" للماكينة الشغّالة الآن) هو Feature جديد يحتاج طلبًا صريحًا، وليس جزءًا من قاعدة منتصف الليل.

## REMAINING TASKS (بالترتيب الصحيح)

1. ~~مراجعة المستخدم لواجهة أيام/أسابيع العمل الجديدة~~ — **تم تجاوزه بطلب صريح من المستخدم**. لا يزال **مطلوبًا** قبل الاعتماد الإنتاجي.
2. ~~**Multi-Select**~~ — **مكتمل.** لم يُختبَر بصريًا بعد.
3. ~~**Bulk Move / Bulk Delete**~~ — **مكتمل.** لم يُختبَر بصريًا ولا حيًا بعد.
4. ~~**Ctrl+V**~~ — **مكتمل نهائيًا (COMPLETED)،** لا لصق جماعي إطلاقًا.
5. ~~**Notifications**~~ — **مكتمل.** لم يُختبَر بصريًا ولا حيًا بعد.
6. ~~**Statistics** (أسبوعية/شهرية) + **Operator Statistics** + **Operator Sessions** + **قاعدة منتصف الليل**~~ — **مكتمل بالكامل الآن 🆕 (بما فيها قاعدة منتصف الليل الحقيقية، أُكمِلت هذه الجلسة).** لم يُختبَر حيًا/بصريًا بعد — هذا الآن **ست** ميزات متراكمة بدون أي اختبار حقيقي بينها.
7. ~~**استبدال Settings بـStatistics**~~ — **مكتمل 🆕.** `SettingsPage` (الديكورية) وعنصرها في `navAdmin` ونقطة الـrender الخاصة بها **حُذِفت بالكامل** من `frontend/index.html` بناءً على طلب صريح من المستخدم بالمتابعة رغم عدم الاختبار الحي لأي من الميزات الست السابقة. Statistics هي الآن الصفحة الوحيدة في مكانها (كانت مربوطة وظيفيًا مسبقًا). تحقّق Babel نجح. **لم يُختبَر بصريًا/حيًا بعد** — أصبحت الآن **سبع** ميزات متراكمة بدون اختبار حي.
8. ~~**تحسينات Socket.io / Auto-Sync الإضافية**~~ — **مكتمل 🆕.** المستخدم وضّح المطلوب صراحةً: لا حاجة لـF5 لرؤية التحديثات، مع Auto-Sync احتياطي عند انقطاع الاتصال. نُفِّذ كما يلي — انظر تفاصيل كاملة في قسم "Socket.io / Auto-Sync" أدناه. **هذا يُغلِق آخر بند من الخطة الأصلية ذات الـ30 مرحلة على مستوى الكود.**

## LAST WORKING STATE

- **Current Phase:** جلسة **تنفيذ بند 8 (Socket.io/Auto-Sync)** — آخر بند من الخطة الأصلية ذات الـ30 مرحلة، مباشرة بعد جلسة "حذف SettingsPage" (بند 7). لم تُلمَس أي Feature أخرى (Ctrl+V، Bulk، Work Days، Multi-Select، Notifications، Statistics نفسها، الـBackend عدا `users.js`) في هذه الجلسة.
- **القرار المأخوذ من المستخدم:** طُلِب توضيح بند 8 صراحةً (كان غامض النطاق)، فأوضح المستخدم: الهدف هو ألا يحتاج أي مستخدم لضغط F5 لرؤية أي تحديث (مثال أعطاه: رفع ملف من مستخدم يظهر فورًا عند الآخرين — وهذا **كان موجودًا بالفعل**)، بالإضافة لطلب Auto-Sync احتياطي يعمل بشكل دوري **تحديدًا عند انقطاع اتصال Socket.io** كخط دفاع ثانٍ. هذا توضيح كافٍ وواضح للنطاق، فتم التنفيذ مباشرة دون سؤال إضافي.
- **Current Status:** بند 8 **مكتمل على مستوى الكود**. الفحص الشامل لكل نقاط بث Socket.io في الباك إند أظهر أن الفجوة الحقيقية الوحيدة كانت `users.js` (إضافة/حذف مستخدم لا يبثّان شيئًا) — بقية النظام (كل الـ19 endpoint في `machines.js`, `workdays.js`, `events.js`) كان يبث بالفعل بلا نقص. تم إصلاح `users.js` (حدثا `user:new`/`user:deleted` لغرفة `admins`) وربطهما في الفرونت. كذلك أُضيف Auto-Sync احتياطي في `App` (`frontend/index.html`): `setInterval` كل 20 ثانية يعمل فقط أثناء `disconnect`، يتوقف فور `connect` مع جلب فوري واحد للّحاق. Statistics بقيت بلا Socket.io كما كانت (لم يطلب المستخدم تغيير ذلك تحديدًا). **غير مختبَر حيًا/بصريًا بعد** — التحقق المتاح فقط: `node --check` على `users.js` (نجح)، `@babel/core transformSync` على كامل الفرونت بعد التعديل (نجح بدون أخطاء نحوية).
- **Confirmed VERIFIED (هذه الجلسة):** بحث شامل (`grep`) عن كل `io.emit`/`io.to(...).emit` في الباك إند كله قبل أي تعديل — أكّد أن `machines.js` (19 endpoint) و`workdays.js` و`events.js` كانت تبث بالفعل، ولا وجود لأي بث في `users.js`/`auth.js`/`notifications.js` قبل التعديل. بعد التعديل: `users.js` فقط أصبح يبث حدثين جديدين. لم يُغيَّر أي ملف SQL أو migration. Statistics، Ctrl+V، Bulk، Work Days، Multi-Select، Notifications — لم تُلمَس إطلاقًا هذه الجلسة.
- **Confirmed DONE (code-level only, NOT visually/live tested by anyone yet):** كل بنود الخطة الأصلية ذات الـ30 مرحلة (Branding، Work Days/Weeks، Multi-Select، Bulk Move/Delete، Ctrl+V، Notifications، Statistics بقاعدة منتصف الليل، حذف Settings، 🆕 Socket.io/Auto-Sync). **لا يوجد أي بند "غير منجز على مستوى الكود" متبقٍ من الخطة الأصلية.**
- **Confirmed NOT DONE:** لا شيء من الخطة الأصلية. **المتبقي بالكامل هو الاختبار الحي/البصري** — تسع (تقريبًا) ميزات كبيرة متراكمة الآن بدون أي اختبار حي منذ بداية المشروع في جلسات الـAI.
- **Last Modified Files (هذه الجلسة):** `backend/src/routes/users.js` (إضافة `io.to('admins').emit('user:new'/'user:deleted', ...)` في مساري الإنشاء والحذف)، `frontend/index.html` (مستمعا `user:new`/`user:deleted` جديدان + منطق Auto-Sync الاحتياطي `disconnect`/`connect` داخل `useEffect` الخاص بـSocket.io في `App`). لا تغيير في أي ملف SQL أو migration.
- **Current Error:** لا يوجد معروف على مستوى الكود. لا حاجة لأي migration لهذه الجلسة.
- **Next Exact Action:** لا يوجد بند كود جديد متبقٍ من الخطة الأصلية. الخطوة التالية المنطقية الوحيدة هي **تشغيل حي كامل واختبار بصري** لكل الميزات المتراكمة (كل ما هو موثَّق أعلاه كـ"code-level only, NOT tested") — هذا أصبح الآن أولوية قصوى نظرًا لحجم التراكم الكبير جدًا. إن طلب المستخدم أي ميزة جديدة غير مذكورة في الخطة الأصلية، فهذا يُعتبر توسيعاً جديدًا للنطاق يحتاج توثيقاً كقرار صريح منه، وليس استكمالاً للخطة القديمة.

## CONTINUATION PROMPT

للجلسة القادمة، ابدأ بهذا الأمر بالضبط:

```
اقرأ HANDOFF.md كاملاً من أوله لآخره (لا تكتفِ بقسم واحد). إن وجدت أي تعارض
بين أقسامه، الكود الفعلي هو الفيصل دائمًا — افحصه بنفسك، ثم وثّق التعارض
ووحّد الملف فورًا بدل تجاهله.

تحقّق تحديدًا من هذا (لا تفترض، افحص):
- frontend/index.html — تأكد أنه لا يوجد أي مرجع متبقٍ لـSettingsPage أو
  page==="settings" أو id:"settings" (بحث نصي شامل). تأكد أن StatisticsPage
  لا تزال مربوطة فعليًا (page==="statistics" && user?.role==="admin").
- backend/src/routes/users.js — تأكد من وجود io.to('admins').emit('user:new', ...)
  بعد الإنشاء وio.to('admins').emit('user:deleted', ...) بعد الحذف، وأنهما لا
  يزالان مستخدَمين فعليًا (وليسا معرَّفين وغير مستخدَمين أو حُذِفا بالخطأ).
- frontend/index.html — تأكد من وجود مستمعي socket.on("user:new",...) و
  socket.on("user:deleted",...) داخل useEffect الخاص بـSocket.io في App، وأن
  منطق disconnect/connect لـAuto-Sync الاحتياطي (fallbackInterval، 20 ثانية،
  hasConnectedBefore) لا يزال موجودًا وسليمًا.
- شغّل تحقق Babel/esbuild على سكربت الفرونت و`node --check` على أي ملف
  Backend عُدِّل، بعد أي تعديل جديد قبل التسليم (لا يوجد بديل عن هذا التحقق
  طالما لا توجد بيئة متصفح حقيقية متاحة).

ملاحظات ثابتة غير قابلة لإعادة الفتح (بدون طلب صريح جديد من المستخدم):
- Ctrl+V: لصق لملف واحد فقط عبر pasteTargetId، لا لصق جماعي إطلاقًا.
- قاعدة منتصف الليل: تقسيم حقيقي بالثانية عبر splitIntervalByDay، وليس نسب
  المدة كاملة ليوم البداية.
- الإحصائيات اليومية/المجمَّعة الجديدة تمر دائمًا عبر splitIntervalByDay،
  وليس GROUP BY DATE_FORMAT مباشر.
- SettingsPage محذوفة نهائيًا — لا تُعِد إضافتها أو أي صفحة إعدادات ديكورية
  مشابهة دون طلب صريح جديد.
- Auto-Sync الاحتياطي (فرونت) مشروط بانقطاع الاتصال فقط (20 ثانية أثناء
  disconnect، يتوقف عند connect) — لا توسّعه إلى Polling دائم، ولا تُضِفه
  لصفحة Statistics، دون طلب صريح جديد.

اسأل المستخدم صراحةً: هل شغّل السيرفر وراجع بصريًا أيًا من الميزات المتراكمة
الآن (Work Days / Multi-Select / Bulk Move-Delete / Ctrl+V / Notifications /
Statistics بما فيها قاعدة منتصف الليل / حذف Settings / Socket.io Auto-Sync
الجديد)؟ لا تفترض عدم المراجعة ولا افتراض المراجعة — اسأل. إن طلب المتابعة
رغم عدم المراجعة، اتبع رغبته ووثّق ذلك بوضوح في HANDOFF.md كقرار منه، لكن
أعد التنبيه بوضوح لحجم التراكم غير المختبَر (كل بنود الخطة الأصلية الآن
مكتملة على مستوى الكود، ولا شيء منها اختُبِر حيًا حتى الآن — هذا أهم بند
معلّق في المشروع).

**لا يوجد بند كود جديد من الخطة الأصلية ذات الـ30 مرحلة متبقٍ** — كل البنود
(1 إلى 8) مكتملة على مستوى الكود. إن لم يطلب المستخدم ميزة جديدة صراحةً،
فالخطوة الوحيدة المنطقية هي اقتراح اختبار حي شامل، وليس اختراع مهمة كود
جديدة. إن طلب ميزة جديدة، فهي توسيع جديد للنطاق يحتاج توثيقاً كقرار صريح
منه في قسم PROJECT OVERVIEW وREMAINING TASKS (كبند 9 جديد)، وليس استكمالاً
لبند قديم.

بعد الانتهاء، حدّث HANDOFF.md بدقة (الحالة الفعلية فقط، لا ادّعاءات غير
مختبَرة)، وتأكد أن كل الأقسام (المقدمة، FILES ACTUAL STATE، TESTING STATUS،
REMAINING TASKS، LAST WORKING STATE) متسقة مع بعضها قبل التسليم.
```

---

## 🆕 نتائج الاختبار الحي (هذه الجلسة — أول اختبار حي حقيقي في المشروع)

**البيئة:** حاوية Ubuntu 24.04 تابعة لجلسة الـAI، MariaDB 10.11 مثبَّتة ومُشغَّلة فعليًا
محليًا (وليست Aiven الإنتاجية)، Node.js v22، سيرفر `node src/server.js` حقيقي شغّال،
اختبارات عبر `curl` حقيقي و`socket.io-client` حقيقي (سكربتات Node مستقلة، ليس متصفحًا).
**قيد مكتشَف:** كل استدعاء أداة Shell في بيئة هذه الجلسة يعمل في عملية معزولة — أي عملية
خلفية (كسيرفر Node أو MariaDB) تُقتَل بين استدعاء وآخر. الحل: كل تسلسل اختبار (إعداد +
تشغيل سيرفر + طلبات) نُفِّذ داخل استدعاء واحد متصل. هذا القيد خاص ببيئة تنفيذ الـAI نفسها
وليس له علاقة بالمشروع أو بأي استضافة حقيقية له.

### 1) الإعداد
- ✅ ثُبِّتت MariaDB 10.11 فعليًا (`apt-get install mariadb-server`) وشُغِّلت (`service
  mariadb start`).
- ✅ **تحقّق فعلي (وليس افتراضًا):** فُحِص `database.sql` بأمر `grep "CREATE TABLE"` وأكّد
  وجود كل الجداول التسعة المطلوبة (`users, machines, machine_files, work_weeks,
  work_days, logs, machine_runtime_logs, operator_sessions, notifications`) — لا حاجة
  لأي ملف migration منفصل فوقه، كما زعم هذا الملف سابقًا (تأكيد الآن بالفحص الفعلي، لا
  الاعتماد على الادّعاء وحده).
- ✅ أُنشئت قاعدة بيانات جديدة (`tashkeel_cnc`) ومستخدم DB مخصَّص (وليس root، أقرب لواقع
  النشر الحقيقي)، واستُورِد `database.sql` بنجاح كامل بدون أي خطأ.
- ✅ `backend/.env` أُنشئ من `.env.example`، `R2_*` تُركت فارغة → السيرفر استخدم القرص
  المحلي تلقائيًا كما هو متوقَّع (رسالة تحذير واضحة عند الإقلاع تؤكد ذلك).
- ✅ `npm install` نجح (179 حزمة).
- ✅ `node src/utils/seed.js` نجح — أنشأ `lol`/`214205` (Root Admin) كما هو موثَّق في
  الملف نفسه.
- ✅ السيرفر عند الإقلاع طبع رسالة نجاح اتصال قاعدة البيانات **بدون أي خطأ**.
- ⚠️ **ملاحظة أمان يستحق انتباه المستخدم (وليست خطأ برمجي):** `seed.js` يحتوي كلمة مرور
  Root Admin الفعلية (`214205`) نصًا صريحًا (Plaintext) في الكود المصدري نفسه، وهي تبدو
  مطابقة لبيانات دخول النظام الحقيقي الفعلي (وليست بيانات اختبار وهمية). يُنصَح بشدة
  بإخراج هذه القيمة إلى متغيّر بيئة (`.env`) بدل كتابتها في الكود مباشرة، خصوصًا أن هذا
  الملف يُرفَع كجزء من كل تسليم `.zip`.

### 2) اختبار الـBackend عبر HTTP حقيقي — **كل الحالات نجحت، صفر Bugs**
| الاختبار | النتيجة |
|---|---|
| `POST /api/auth/login` | ✅ نجح، JWT صالح مُستلَم ومُستخدَم في كل الطلبات التالية |
| `GET /api/machines` | ✅ الشكل مطابق تمامًا (`big`/`small`/`queue`) |
| رفع ملف حقيقي (PDF) لماكينة `big` | ✅ نجح، `hasFile:true`, `fileSize` صحيح |
| رفع ملف حقيقي (PNG) لماكينة `small` | ✅ نجح |
| نقل ملف فردي (`PATCH /files/:fileId/move`) | ✅ نجح، أعاد `source`+`target` محدَّثين |
| **Bulk move (`PATCH /files/bulk-move`)** | ✅ نجح (`movedCount:1, skippedIds:[]`) |
| **Bulk delete (`DELETE /files/bulk-delete`)** | ✅ نجح (`deletedCount:1, skippedIds:[]`) |
| **تحقّق ترتيب المسارات (bulk-move/bulk-delete قبل `/files/:fileId` العام)** | ✅ **مؤكَّد حيًا** — لم يُلتقَط أي من الاثنين كـ`fileId` حرفي، لا تعارض إطلاقًا |
| إضافة مستخدم (`POST /api/users`) | ✅ نجح |
| حذف مستخدم (`DELETE /api/users/:id`) | ✅ نجح |
| `POST /api/work/days`, `GET /api/work/days`, `GET /api/work/days/active` | ✅ كلها نجحت |
| `POST /api/work/weeks` | ✅ نجح |
| `GET /api/notifications`, `GET /api/notifications/all` | ✅ كلاهما نجح بالشكل الصحيح |

**لم يُعثَر على أي Bug حقيقي في أي endpoint من هذه القائمة.**

### 3) اختبار قاعدة منتصف الليل — **الأهم، ونجح تمامًا**
أُدخِل صف حقيقي في `machine_runtime_logs` بـ`started_at='2026-09-13 23:00:00'`,
`stopped_at='2026-09-14 02:00:00'` (يعبر منتصف الليل فعليًا، بفارق 3 ساعات)، ثم طُلِب
`GET /api/statistics/machines?scope=week&weekId=1`:
- النتيجة: `daily: [{date:"2026-09-13", big:3600}, {date:"2026-09-14", big:7200}]`
- **مطابق تمامًا للحساب اليدوي المطلوب في مهمة الاختبار: ساعة واحدة (3600 ثانية) لليوم
  الأول، ساعتان (7200 ثانية) لليوم الثاني، إجمالي 10800 = 3 ساعات كاملة. لا تسرّب لكل
  المدة على يوم البداية.** ✅ **قاعدة منتصف الليل تعمل بشكل صحيح 100% في اختبار حي حقيقي.**
- تكرار الاختبار على `/api/statistics/operators` (جلسة مشغّل 22:30 → 01:15 اليوم التالي،
  2:45 ساعة) أعاد `totalSeconds:9900` (=02:45:00) بشكل صحيح أيضًا — نفس دالة التقسيم
  (`splitIntervalByDay`) تُستخدَم في المسارين، والنتيجة الإجمالية صحيحة في الحالتين.

### 4) اختبار Socket.io عبر عميل حقيقي (`socket.io-client`, ليس متصفحًا) — **كل الأحداث الثمانية وصلت**
سكربت Node مستقل اتصل بتوكن JWT صالح واستمع للأحداث الثمانية المطلوبة، بينما طلبات HTTP
منفصلة نفّذت الإجراءات المقابلة:

| الحدث | نتيجة | كيف تم تحفيزه |
|---|---|---|
| `machine:update` | ✅ وصل | رفع ملف |
| `log:new` | ✅ وصل (٥ مرات مختلفة) | كل عملية تُسجِّل حدثاً |
| `notif:new` | ✅ وصل | رفع ملف + بدء يوم عمل |
| `presence:update` | ✅ وصل | اتصال العميل نفسه |
| `workday:update` | ✅ وصل | `POST /api/work/days` |
| `workweek:new` | ✅ وصل | `POST /api/work/weeks` |
| `user:new` 🆕 | ✅ **وصل** | `POST /api/users` — **يؤكد إصلاح الجلسة السابقة يعمل حيًا** |
| `user:deleted` 🆕 | ✅ **وصل** | `DELETE /api/users/:id` — **يؤكد إصلاح الجلسة السابقة يعمل حيًا** |

كل الأحداث الثمانية وصلت خلال أقل من ثانية من التحفيز. **لا Bug في بث Socket.io.**

### 5) اختبار Auto-Sync الاحتياطي — **حي فعليًا، بقطع/استعادة سيرفر حقيقيين**
منطق الكود (الأسطر 2177–2196 من `frontend/index.html`) نُسِخ حرفيًا (نفس الأسطر
تمامًا) في سكربت Node موصول بعميل `socket.io-client` حقيقي، ثم:
1. قُتِلت عملية السيرفر فعليًا (`kill -9`) بعد 6 ثوانٍ من الاتصال → حدث `disconnect` انطلق
   فعليًا عند العميل، وبدأ `fallbackInterval` (20 ثانية) كما هو متوقَّع.
2. مع بقاء السيرفر معطَّلاً 45 ثانية، سُجِّلت نقطتا استدعاء لـ`catchUpSync` عند **25.9
   ثانية** و**45.9 ثانية** من بداية الاختبار — أي كل 20 ثانية بالضبط بعد الانقطاع (عند
   5.9 ثانية)، تمامًا كما هو مطلوب. لا Polling قبل الانقطاع إطلاقًا (صفر استدعاءات بين
   0 و25.9 ثانية).
3. أُعيد تشغيل السيرفر فعليًا → حدث `connect` انطلق عند **53.0 ثانية**، واستدعى
   `stopFallback()` فورًا (تأكيد: `fallbackIntervalStillActive: false` في نهاية
   الاختبار) **ونفَّذ جلبة `catchUpSync` واحدة فورية للّحاق بما فات** — تمامًا كما هو
   موثَّق أنه يجب أن يحدث.
- **✅ Auto-Sync الاحتياطي يعمل بدقة 100% مطابقة للمواصفة الموثَّقة: مشروط بالانقطاع فقط،
  كل 20 ثانية بالضبط، يتوقف فورًا عند العودة مع جلبة لحاق واحدة.**

### ما لم يُختبَر في هذه الجلسة (قيود بيئية، وليست إخفاقات في الكود)
- ❌ **لا توجد بيئة متصفح حقيقية (لا headless ولا غيرها) متاحة في بيئة تنفيذ الـAI هذه** —
  لذلك لم يُختبَر الفرونت بصريًا فعليًا (عرض React في متصفح، ضغط أزرار Multi-
  Select/Ctrl+V الفعلي بالماوس ولوحة المفاتيح، إلخ). كل ما اختُبِر هو منطق الـBackend
  الكامل + منطق Auto-Sync في الفرونت (منسوخ ومُشغَّل مباشرة في Node، ضد سيرفر حقيقي).
  **يبقى مطلوبًا من المستخدم:** فتح الواجهة في متصفح حقيقي (بعد نشر هذا الإصدار أو
  تشغيله محليًا) والتحقق البصري النهائي.
- ❌ Ctrl+V تحديدًا لم يُعَد اختباره حيًا هذه الجلسة (يتطلب متصفحًا فعليًا لمحاكاة
  الصق حافظة — لا يوجد اختبار Backend مقابل له لأنه منطق فرونت بالكامل).
- ❌ التحقق البصري لـBranding (الشعار/الألوان) في متصفح فعلي.
- **لم يُعثَر على أي Bug حقيقي يحتاج إصلاحاً في أي شيء اختُبِر هذه الجلسة** — لذلك لم
  يُعدَّل أي ملف كود في المشروع (فقط هذا الملف `HANDOFF.md` تم تحديثه بالنتائج).

### الخلاصة النهائية
**كل منطق الـBackend والـSocket.io لكل الميزات التسع المتراكمة اختُبِر حيًا بنجاح تام ضد
بنية تحتية حقيقية (MariaDB + Node + socket.io-client)، بدون العثور على أي Bug واحد.**
المتبقي الوحيد هو التحقق البصري في متصفح حقيقي (خارج قدرة بيئة تنفيذ الـAI الحالية) —
وهذا الآن أولوية للمستخدم تحديدًا، وليس للـAI في جلسة قادمة، طالما لا تتوفر بيئة متصفح.
