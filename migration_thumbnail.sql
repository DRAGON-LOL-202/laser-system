-- ============================================================
-- ترقية قاعدة بيانات موجودة مسبقاً (لا تحذف أي بيانات)
-- شغّل هذا الملف فقط إذا كانت قاعدة بياناتك منشأة قبل إضافة ميزة "صورة المعاينة"
-- مثال للتشغيل: mysql -u root -p اسم_قاعدة_البيانات < migration_thumbnail.sql
-- ============================================================

ALTER TABLE `machine_files`
  ADD COLUMN `thumbnail_filename` VARCHAR(255) DEFAULT NULL COMMENT 'اسم صورة المعاينة المخزنة (يرفعها المشرف فقط)'
  AFTER `file_size`;
